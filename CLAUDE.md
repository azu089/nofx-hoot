# CLAUDE.md - Hoot AI 开发规范

> 本文档适用于所有在 Hoot 项目工作的 AI 助手
> 版本: v1.3 | 项目: Hoot - AI 量化交易平台
> 代币名称: HOOT

---

## 本地开发配置

| 服务 | 端口 | 说明 |
|------|------|------|
| 前端 (Next.js) | **3001** | `pnpm dev` |
| 后端 API (NestJS) | **4001** | NestJS 后端 |
| PostgreSQL | **5433** | 数据库 |
| Redis | **6379** | 缓存 |

---

## !!!!! 铁律：修改逻辑前必须全局理解 !!!!!

**修改任何逻辑代码之前，禁止靠猜，必须：**

1. **读完整相关代码**：找到所有相关文件完整阅读，不能只看片段或报错行
2. **输出评估结论**：根本原因 + 影响范围 + nofx 对比（涉及网格/交易时）+ 修改方案
3. **等 PM 确认**：输出评估后等确认，再写代码

**禁止行为**：
- 看到现象就加规则（e.g., AI 只挂2格 → 直接加"必须挂所有层"）
- 未追查根本原因就修改实现（e.g., 只改 pendingNotional 而不理解 capUsed 全链路）
- 每次修 bug 都向 prompt 堆规则（积累过度约束，干扰 AI 判断）

详见：`.claude/rules/开发工作流.md`

---

## !!!!! 强制执行 - 每次会话必做 !!!!!

**在执行任何任务之前，AI 必须先完成以下步骤（无例外）：**

### Step 1: 读取核心文件（必须用 Read 工具）
```
1. 文档/核心文档/开发顺序.md (前 300 行) - 确认当前阶段
2. 文档/核心文档/项目需求.md (前 200 行) - 理解业务背景
```

### Step 2: 输出当前状态摘要
```
## 当前开发状态
- 当前阶段: Phase X
- 当前进度: [描述]
- 下一步任务: [任务名称]
```

### Step 3: 使用 TodoWrite 记录任务
```
- 将本次任务拆解为具体 todo 项
- 标记 in_progress / completed
```

**违反以上规则 = 交付不合格，PM 必须打回！**

---

## 一句话总纲

> **先计划、后代码；只做原子任务；白名单护栏；验收可视化；失败可回滚；环境要上报；文档即代码；选型要保守；两次失败就冻结范围最小修复。**

---

## 0. 总目标与完成定义

### 0.1 DoD 硬标准（Definition of Done）

一次交付完成，必须**同时满足以下 6 条**：

| # | 条件 | 说明 |
|---|------|------|
| 1 | **可用能力** | 本轮只交付 1 个原子任务（一个接口/脚本/页面/配置变更） |
| 2 | **肉眼可见成功信号** | 必须能用 curl/Postman/页面/日志 证明功能已生效 |
| 3 | **验收步骤** | 提供明确"怎么做 + 预期看到什么"（不接受"应该没问题"） |
| 4 | **回滚方案** | 说明如何撤销本次改动，回到上一个稳定版本 |
| 5 | **风险与影响说明** | 说明潜在风险点、影响范围（禁止隐性变更） |
| 6 | **文档同步** | 涉及配置/依赖/环境变量变化，必须同步更新文档 |

**不满足以上任意一条：一律视为"未完成"，必须打回。**

### 0.2 全局三条底线（永远不破）

1. **先闭环再美化**：先让核心流程跑通、数据/账/权限正确，再做 UI/动效/重构
2. **先原型后功能**：先出可点击原型，再接真实数据/API/支付/链上
3. **永远可回滚**：任何修改都要能回退；没有回滚说明禁止合并/上线

### 0.3 必读文件清单

每次开始新任务时，AI 必须先阅读（如存在）：

- `CLAUDE.md`（本文件）
- `文档/核心文档/项目需求.md`
- `文档/核心文档/开发顺序.md`
- `文档/核心文档/开发助手.md`
- `README.md` / `DEV_Log.md`

若对当前任务不清楚：**先提问，不猜、不乱改**。

---

## 1. 核心工作模式

### PM 的工作方式
PM 只用中文描述需求，不需要记任何命令。AI 自动判断该做什么。

**PM 只需要：**
- 用中文说想要什么功能
- 用中文描述遇到的问题
- 最后看网页确认结果

**AI 自动完成：**
- 分析需求，判断是开发/修复/测试
- 执行所有技术操作
- 验收前自己确认功能可用

### 自动执行原则
- 读取文件 -> 直接读，不问
- 编辑代码 -> 直接改，不问
- 运行测试 -> 直接跑，不问
- 启动服务 -> 直接启动，不问
- 修复报错 -> 直接修，不问
- 删除重要文件 -> **必须问**
- 修改 .env -> **必须问**
- git push -> **必须问**

---

## 2. 验收前必须完成

在告诉 PM "打开网页验收" 之前，AI 必须自己完成：

1. **确认服务运行中** - `docker compose ps`
2. **确认代码无报错** - `pnpm build` 或 `npx tsc --noEmit`
3. **确认接口可访问** - `curl http://localhost:4000/api/health`
4. **确认页面可打开** - 前端服务在运行

**只有以上全部通过，才能告诉 PM 去看网页。**

---

## 3. 输出格式

任务完成后，必须输出：

```
## 任务完成

### 做了什么
- [具体改动1]
- [具体改动2]

### 已验证
- 服务运行中
- 代码无报错
- 接口可访问

### 验收方式
打开网页：http://localhost:xxxx/xxx
应该看到：[具体描述]
```

---

## 4. 技术栈约束 (QuantFi 专用)

| 类别 | 技术选型 |
|------|---------|
| 语言 | TypeScript |
| 后端 | NestJS |
| 前端 | Next.js 14 (App Router) |
| 包管理 | pnpm (workspace monorepo) |
| 数据库 | PostgreSQL 15 |
| 缓存 | Redis 7 |
| 容器化 | Docker Compose |
| 云服务 | DigitalOcean (VPS 编排) |
| 对象存储 | S3 (数据备份) |

**禁止随意引入：**
- 未经批准的新框架
- Beta / Alpha 版本依赖
- 微服务架构（V1 阶段）
- Kafka / RabbitMQ（V1 不需要）

### 端口分配规范（固定，禁止修改）

| 服务 | 端口 | 说明 |
|------|------|------|
| 前端 (next-forge app) | **3001** | Next.js 开发服务器 |
| 后端 API (NestJS) | **4001** | NestJS 后端 |
| PostgreSQL | **5433** | 数据库 |
| Redis | **6379** | 缓存 |

**重要：前端永远使用 3001 端口，禁止使用 3000！**

### V0 UI 生成规则（强制）

**高保真 UI 必须通过 V0 API 生成，生成后可以修改调整。**

```bash
# 生成 UI 组件
pnpm v0:generate <组件名> "<需求描述>"

# 获取生成的代码
pnpm v0:fetch <chat-id> <组件名>
```

详细规则见：`.claude/rules/V0-UI规则.md`

---

## 5. 6 段固定结构

每轮开发回复必须包含：

1. **本轮任务理解（≤5行）** - 只做什么/不做什么
2. **变更计划 & 文件列表** - 允许改动的白名单
3. **实现内容（按文件分段）** - 具体代码改动
4. **验证步骤** - 执行目录 + 命令 + 预期输出
5. **风险点 & 自检清单** - 影响范围 + 潜在风险
6. **回滚方案** - 如何撤销本次变更

---

## 6. 禁止事项

- 顺手优化 / 顺手重构
- 跨模块改动（单任务只动一个模块）
- 默认追新（禁止安装最新版依赖）
- 硬编码密钥或敏感信息
- 不提供回滚方案的修改
- 写死本机绝对路径（如 C:\Users\...、/Users/...）

### 6.1 注释中禁止出现的内容（开源/商用前置硬规则）

HOOT 未来要开源 + 商用，代码注释中**绝对禁止**出现以下任何表述：

- "对齐改版" / "参照改版" / "模仿改版" / "学改版" / "与改版差异"
- "改版的 X" / "改版直接 X" / "改版用 X" / "改版风格"
- "原版 X" / "沿用原版" / "与原版一致" / "回退原版" / "原版硬编码"
- "nofx改版" / "nofx 改版" / "改版的 auto_trader.go" 等指向外部 fork 的表述
- "HOOT 独有" / "HOOT 原创" / "HOOT 首创"（自吹式标签）
- "对标 X" / "参考 X 项目" / "翻译自 X"（任何指向外部参考项目的表述）
- 内部任务编号: "v1.1"、"P0.x"、"P1-x"、"P2-x"、"P3-x"、"P4-x"
- "审计发现" / "审计修复 Bug #N" / "真实策略审计"（内部开发流程）
- "HOOT nofx 升级 2026-04-X" 之类的工作元信息
- 任何明示或暗示"从别处借鉴"的技术考古说明

**允许保留的注释**:
- 纯技术说明（算法/公式/参数含义）
- 行为保证（default disabled / nil-safe / thread-safe）
- 灰度开关用法
- 设计意图（为什么这样实现，不提外部对比）
- 行内 TODO/FIXME 但必须用技术语言

**写注释的正确心态**:
把代码当作**从零自研**的作品写注释。读者应该感觉这是 HOOT 原生设计，
不应该察觉到任何外部参考来源或内部开发过程痕迹。

**违反后果**: 交付不合格，必须立即清理 + 重新 commit。

**特别说明**: arena/ 目录里存在的 "Python 原版" 指向 TradingAgents 学术论文
实现，属于开源社区的公开引用（类似学术 cite），不在本禁令范围。新增代码
也禁止新建此类引用，除非是明确的开源项目授权归属声明。

---

## 7. 时间盒与小步提交

### 时间盒（强制）
- 单任务时间盒：30-60 分钟
- 超时必须停止，输出：当前进度 + 卡点 + 下一步建议

### 小步提交（强制）
- 单次提交建议 ≤ 5 个文件
- 每个变更配：自检清单 + 验收步骤 + 回滚步骤

---

## 8. 成本与配额护栏

任何第三方 API / 模型调用 / 链上 RPC / 付费服务，必须写清：
- 配额限制
- 失败策略
- 预估调用量与成本上限
- 达到上限时的降级策略（缓存/降频/只读模式）

---

## 9. AI 幻觉防护

### 来源标注（强制）
任何新增调用（库/方法/API）必须标注：
- 来自哪个包/模块
- 是否需要安装/启用
- 若第三方 API：需要哪些配置项 + 失败兜底

### 先列清单再写代码
对复杂功能：先输出"将要新增的函数/模块/文件清单"再写代码，防止凭空捏造方法名。

---

## 10. 测试用例最小集

每个原子任务至少提供 3 条测试用例：

| 类型 | 说明 |
|-----|------|
| 正常路径 | 成功场景 |
| 异常路径 | 输入非法/权限不足/依赖失败 |
| 边界路径 | 空值/极值/超时/特殊字符 |

涉及资金/结算/计费：必须补充对账/验算脚本。

---

## 11. 修复前必须全局理解

修复任何 bug 前，必须先做：

1. **理解问题根源** - 不只看报错，要理解为什么
2. **分析影响范围** - 这个模块被谁调用？
3. **检查相关代码** - 读取相关文件，理解整体逻辑
4. **制定最小修复方案** - 改动越少，风险越小

---

## 12. 可恢复性优先

- 任一步骤失败，都要保证可以回退到上一个"已知可运行版本"
- 用户端尽量不中断（尤其资金、支付、登录、关键业务）
- 关键错误日志可定位（含 request_id / trace_id）
- 关键配置通过环境变量管理（禁止硬编码密钥）
- 失败有明确兜底（超时/重试/错误提示/降级）

---

## 13. 夜间工作模式

触发词：
- "今晚帮我做..."
- "夜间任务..."
- "明早验收..."

规则：
- 一口气做完所有任务
- 遇到问题自己解决或跳过
- 生成日志到 `文档/夜间日志/`
- 禁止架构变更、引入新依赖

---

## 14. QuantFi 业务特殊规则

### 14.1 资金安全验算要求

所有涉及资金操作的代码，必须：
- 使用 DECIMAL 类型，禁止 FLOAT
- 提供验算公式
- 容忍误差：0（必须精确匹配）
- 验算脚本位置：`scripts/验算/`

### 14.2 计费幂等性

- 所有计费操作必须使用 `unique_order_id`
- 格式：`{type}_{user_id}_{timestamp}_{nonce}`
- 数据库唯一约束确保物理杜绝重复扣费

### 14.3 API Key 安全

- 必须使用 AES-256-GCM 加密
- 存储字段：`encrypted_blob` + `iv` + `auth_tag`
- 严禁明文落地，仅在 Docker 启动时注入内存

### 14.4 单租户隔离

- 每个 VIP 用户 = 1 独立 VPS
- 心跳检测：15 分钟无心跳 = 僵尸节点 -> 销毁 -> 退款
- VPS 销毁前必须备份到 S3

---

---

## 15. 傻瓜式启动/执行指令（强制）

AI 给出的任何命令必须同时写清：

1. **在哪个目录执行**（仓库根目录/某子目录）
2. **是否需要权限**（管理员/sudo）与前置条件
3. **预期看到什么输出才算成功**（关键日志/exit code/打开页面）

**禁止只说"你运行一下"或只给命令不说明执行位置与成功信号。**

示例：
```
执行目录：/Users/azu/主QuantFi
命令：docker compose up -d
成功信号：看到 "Container quantfi-api-1 Started" 且无 error
```

---

## 16. 隐私与安全（强制）

- 禁止把密钥、Token、私钥写入仓库
- 日志脱敏（可打印 uid/request_id，不打印完整密钥/签名/隐私数据）
- 外部接口加速率限制
- 依赖扫描/泄露扫描（如有 CI）

---

## 17. 版本与依赖策略

- **禁止默认安装/升级到"最新版"**
- 新增/升级依赖必须说明：
  - 选择该版本原因（兼容/安全/功能）
  - 是否锁定版本（固定 or 范围）
  - 破坏性变更风险与回滚办法
- 发生依赖冲突：先给兼容矩阵与降级方案，不允许盲改

---

## 18. 文档即代码（强制同步）

必须维护 `README.md` 或 `DEV_Log.md` 作为项目地图。

任何配置变更必须同步更新文档，包含：
- 新增/变更项是什么
- 为什么需要
- 如何配置
- 如何验证
- 如何回滚

**不更新文档 = 交付不完整（DoD 不通过）**

---

## 版本记录

| 版本 | 日期 | 变更说明 |
|-----|------|---------|
| v1.1 | 2025-12-23 | 补全 DoD 硬标准、全局底线、傻瓜式指令等遗漏规则 |
| v1.0 | 2025-12-23 | 初始版本，基于白皮书 v4.0 |
