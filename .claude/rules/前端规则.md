# 前端开发规则（QuantFi 专用）

> 适用于 Next.js 前端开发

---

## 技术栈

| 组件 | 版本 | 说明 |
|------|------|------|
| Next.js | 14.x | 前端框架 (App Router) |
| TypeScript | 5.x | 类型安全 |
| React | 18.x | UI 库 |
| TailwindCSS | 3.x | 样式 |
| Zustand | 4.x | 状态管理 |
| React Query | 5.x | 数据请求 |
| React Hook Form | 7.x | 表单处理 |

---

## 目录结构

```
apps/web/
├── src/
│   ├── app/                 # App Router 页面
│   │   ├── (auth)/          # 认证相关页面
│   │   │   ├── login/
│   │   │   └── register/
│   │   ├── (dashboard)/     # Dashboard 布局
│   │   │   ├── dashboard/
│   │   │   ├── wallet/
│   │   │   ├── trading/
│   │   │   └── gamefi/
│   │   └── layout.tsx
│   ├── components/          # 组件
│   │   ├── ui/              # 基础 UI 组件
│   │   └── features/        # 业务组件
│   ├── hooks/               # 自定义 Hooks
│   ├── lib/                 # 工具库
│   │   ├── api.ts           # API 客户端
│   │   └── utils.ts         # 工具函数
│   ├── stores/              # Zustand 状态
│   └── types/               # 类型定义
└── public/                  # 静态资源
```

---

## 页面状态规范

每个页面必须处理 4 种状态：

| 状态 | 说明 | 处理方式 |
|------|------|---------|
| Loading | 加载中 | 骨架屏/Spinner |
| Empty | 空数据 | 空状态提示 |
| Error | 错误 | 错误提示 + 重试按钮 |
| Success | 成功 | 正常展示数据 |

```tsx
function Dashboard() {
  const { data, isLoading, error } = useQuery(...);

  if (isLoading) return <DashboardSkeleton />;
  if (error) return <ErrorState onRetry={refetch} />;
  if (!data?.length) return <EmptyState />;

  return <DashboardContent data={data} />;
}
```

---

## API 调用规范

### 使用 React Query

```tsx
// 查询
const { data, isLoading } = useQuery({
  queryKey: ['user', userId],
  queryFn: () => api.get(`/users/${userId}`),
});

// 变更
const mutation = useMutation({
  mutationFn: (data) => api.post('/users', data),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['users'] });
    toast.success('创建成功');
  },
  onError: (error) => {
    toast.error(error.message);
  },
});
```

### API 客户端

```typescript
// lib/api.ts
const api = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
  timeout: 10000,
});

// 请求拦截器：添加 token
api.interceptors.request.use((config) => {
  const token = getToken();
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器：处理错误
api.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      // 跳转登录
      router.push('/login');
    }
    return Promise.reject(error);
  }
);
```

---

## 样式规范

### 使用 TailwindCSS

```tsx
// 正确：使用 Tailwind 类
<button className="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded">
  提交
</button>

// 错误：禁止硬编码颜色
<button style={{ backgroundColor: '#3b82f6' }}>
  提交
</button>
```

### 使用 CSS 变量

```css
/* globals.css */
:root {
  --color-primary: #3b82f6;
  --color-secondary: #6b7280;
  --color-success: #10b981;
  --color-error: #ef4444;
}
```

---

## 表单规范

### 使用 React Hook Form

```tsx
const form = useForm<LoginForm>({
  resolver: zodResolver(loginSchema),
  defaultValues: {
    email: '',
    password: '',
  },
});

const onSubmit = async (data: LoginForm) => {
  try {
    await loginMutation.mutateAsync(data);
  } catch (error) {
    form.setError('root', { message: error.message });
  }
};
```

### 表单校验

```typescript
// 使用 Zod
const loginSchema = z.object({
  email: z.string().email('邮箱格式不正确'),
  password: z.string().min(8, '密码至少8位'),
});
```

---

## 安全规范

### 禁止事项

1. **禁止在前端计算资金**（必须后端计算）
2. **禁止存储敏感信息到 localStorage**
3. **禁止直接访问数据库**
4. **禁止硬编码 API URL**（使用环境变量）

### XSS 防护

```tsx
// 正确：React 自动转义
<p>{userInput}</p>

// 危险：必须避免
<div dangerouslySetInnerHTML={{ __html: userInput }} />
```

---

## 国际化

暂时只支持中文，后续可扩展：

```tsx
// 所有用户可见文本使用中文
<button>登录</button>
<span>余额不足</span>

// 错误提示使用中文
toast.error('登录失败，请检查账号密码');
```

---

## 响应式设计

```tsx
// 移动优先
<div className="w-full md:w-1/2 lg:w-1/3">
  {/* 手机全宽，平板一半，桌面三分之一 */}
</div>
```

---

## QuantFi 页面清单

### 普通用户端

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录 | /login | 邮箱+密码 |
| 注册 | /register | 邮箱+密码+邀请码 |
| 仪表盘 | /dashboard | 资产概览 |
| 钱包 | /wallet | 充值/提现/明细 |
| 交易控制台 | /trading | 策略/回测/实盘 |
| GameFi | /gamefi | 积分/质押/兑换 |

### 代理商后台

| 页面 | 路由 | 说明 |
|------|------|------|
| 推广 | /agent/promotion | 链接/海报/邀请码 |
| 业绩 | /agent/performance | 下级/返佣 |

### 管理后台

| 页面 | 路由 | 说明 |
|------|------|------|
| 用户管理 | /admin/users | 封号/重置 |
| VPS 监控 | /admin/instances | 状态/一键停机 |
| 财务审计 | /admin/finance | 提现审核 |
