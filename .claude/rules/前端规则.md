# 前端开发规则（QuantFi 专用）

> 适用于 Next.js 前端开发

---

## ⚠️ 必读文档

**在进行任何前端 UI 开发之前，必须先阅读：**

📄 **`apps/web/src/UI_ARCHITECTURE.md`**

该文档为 QuantFi 前端 UI 架构的**唯一权威规范**，包含：
- 设计理念与视觉规范（配色、字体、间距）
- 完整目录结构（40+ 页面）
- 所有页面功能详细规格
- 组件库清单（28+ UI 组件，50+ 业务组件）
- 状态管理规范（5 个 Zustand Store）
- 交互规范（加载、空态、错误处理）
- 移动端适配（底部导航）

**所有 UI 开发必须严格遵循该文档，不得自行创造新的设计模式或组件。**

---

## 技术栈

| 组件 | 版本 | 说明 |
|------|------|------|
| Next.js | 14.x | 前端框架 (App Router) |
| TypeScript | 5.x | 类型安全 |
| React | 18.x | UI 库 |
| TailwindCSS | 3.x | 样式 |
| Zustand | 4.x | 状态管理 |
| React Query | 5.x | 数据请求 |
| React Hook Form | 7.x | 表单处理 |

---

## 目录结构

> 详细页面规格参见 `UI_ARCHITECTURE.md` 第二章

```
apps/web/src/
├── app/                              # Next.js App Router
│   ├── (public)/                     # 公开页面（无需登录）
│   │   ├── page.tsx                  # Landing 首页
│   │   ├── login/page.tsx
│   │   ├── register/page.tsx
│   │   └── ...
│   ├── (dashboard)/                  # 用户端（需登录）
│   │   ├── layout.tsx                # 含 Sidebar + MobileNav
│   │   ├── dashboard/page.tsx
│   │   ├── strategies/               # 策略市场
│   │   ├── trading/                  # 交易控制台
│   │   ├── wallet/                   # 资产钱包
│   │   ├── gamefi/                   # GameFi 中心
│   │   ├── instances/                # VPS 实例
│   │   └── settings/                 # 设置
│   ├── (agent)/                      # 代理商后台
│   └── (admin)/                      # 管理后台
│
├── components/
│   ├── ui/                           # 基础 UI 组件（28个）
│   ├── layout/                       # 布局组件
│   ├── charts/                       # 图表组件（Recharts）
│   ├── forms/                        # 表单组件
│   └── features/                     # 业务组件（按模块）
│       ├── dashboard/
│       ├── strategies/
│       ├── trading/
│       ├── wallet/
│       ├── gamefi/
│       ├── instances/
│       ├── agent/
│       └── admin/
│
├── hooks/                            # 自定义 Hooks
├── lib/                              # 工具库
│   ├── api.ts                        # API 客户端
│   └── utils.ts                      # 工具函数
├── stores/                           # Zustand 状态（5个）
├── types/                            # 类型定义
└── styles/                           # 样式文件
```

---

## 页面状态规范

每个页面必须处理 4 种状态：

| 状态 | 说明 | 处理方式 |
|------|------|---------|
| Loading | 加载中 | 骨架屏/Spinner |
| Empty | 空数据 | 空状态提示 |
| Error | 错误 | 错误提示 + 重试按钮 |
| Success | 成功 | 正常展示数据 |

```tsx
function Dashboard() {
  const { data, isLoading, error } = useQuery(...);

  if (isLoading) return <DashboardSkeleton />;
  if (error) return <ErrorState onRetry={refetch} />;
  if (!data?.length) return <EmptyState />;

  return <DashboardContent data={data} />;
}
```

---

## API 调用规范

### 使用 React Query

```tsx
// 查询
const { data, isLoading } = useQuery({
  queryKey: ['user', userId],
  queryFn: () => api.get(`/users/${userId}`),
});

// 变更
const mutation = useMutation({
  mutationFn: (data) => api.post('/users', data),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['users'] });
    toast.success('创建成功');
  },
  onError: (error) => {
    toast.error(error.message);
  },
});
```

### API 客户端

```typescript
// lib/api.ts
const api = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
  timeout: 10000,
});

// 请求拦截器：添加 token
api.interceptors.request.use((config) => {
  const token = getToken();
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器：处理错误
api.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      // 跳转登录
      router.push('/login');
    }
    return Promise.reject(error);
  }
);
```

---

## 样式规范

> 完整配色方案参见 `UI_ARCHITECTURE.md` 第一章

### 使用 TailwindCSS + CSS 变量

```tsx
// 正确：使用语义化类名（基于 CSS 变量）
<button className="bg-brand-primary hover:bg-brand-secondary text-white px-4 py-2 rounded-sm">
  提交
</button>

// 错误：禁止硬编码颜色
<button style={{ backgroundColor: '#3b82f6' }}>
  提交
</button>
```

### 官方配色方案（华尔街风格）

```css
/* globals.css - 必须使用这些变量 */
:root {
  /* 背景 */
  --bg-primary: #0B0E11;        /* 主背景 */
  --bg-secondary: #131722;       /* 卡片 */
  --bg-tertiary: #1E222D;        /* 输入框 */

  /* 品牌色 */
  --brand-primary: #3772FF;      /* 科技蓝 */
  --brand-secondary: #2962FF;    /* 深蓝 */

  /* 语义色 */
  --success: #00C087;            /* 盈利/涨 */
  --danger: #F23645;             /* 亏损/跌 */
  --warning: #F7931A;            /* 警告 */

  /* 文字 */
  --text-primary: #FFFFFF;
  --text-secondary: #848E9C;
  --text-tertiary: #5E6673;

  /* 边框 */
  --border-primary: #2B3139;
}
```

### 圆角与间距

```css
--radius-sm: 6px;   /* 按钮/输入框 */
--radius-md: 8px;   /* 小卡片 */
--radius-lg: 12px;  /* 大卡片 */
--radius-xl: 16px;  /* 弹窗 */
```

---

## 表单规范

### 使用 React Hook Form

```tsx
const form = useForm<LoginForm>({
  resolver: zodResolver(loginSchema),
  defaultValues: {
    email: '',
    password: '',
  },
});

const onSubmit = async (data: LoginForm) => {
  try {
    await loginMutation.mutateAsync(data);
  } catch (error) {
    form.setError('root', { message: error.message });
  }
};
```

### 表单校验

```typescript
// 使用 Zod
const loginSchema = z.object({
  email: z.string().email('邮箱格式不正确'),
  password: z.string().min(8, '密码至少8位'),
});
```

---

## 安全规范

### 禁止事项

1. **禁止在前端计算资金**（必须后端计算）
2. **禁止存储敏感信息到 localStorage**
3. **禁止直接访问数据库**
4. **禁止硬编码 API URL**（使用环境变量）

### XSS 防护

```tsx
// 正确：React 自动转义
<p>{userInput}</p>

// 危险：必须避免
<div dangerouslySetInnerHTML={{ __html: userInput }} />
```

---

## 国际化

暂时只支持中文，后续可扩展：

```tsx
// 所有用户可见文本使用中文
<button>登录</button>
<span>余额不足</span>

// 错误提示使用中文
toast.error('登录失败，请检查账号密码');
```

---

## 响应式设计

```tsx
// 移动优先
<div className="w-full md:w-1/2 lg:w-1/3">
  {/* 手机全宽，平板一半，桌面三分之一 */}
</div>
```

---

## QuantFi 页面清单

> 完整页面规格参见 `UI_ARCHITECTURE.md` 第三章

### 公开页面 `(public)`

| 页面 | 路由 | 说明 |
|------|------|------|
| Landing | / | 产品介绍首页 |
| 登录 | /login | 邮箱+密码 |
| 注册 | /register | 邮箱+密码+邀请码 |
| 忘记密码 | /forgot-password | 重置密码 |

### 用户端 `(dashboard)`

| 页面 | 路由 | 说明 |
|------|------|------|
| 仪表盘 | /dashboard | 资产概览、机器人状态 |
| 策略市场 | /strategies | 策略列表、筛选 |
| 策略详情 | /strategies/[id] | 策略配置、订阅 |
| 交易控制台 | /trading | 实盘控制、日志流 |
| 回测系统 | /trading/backtest | 策略回测 |
| 交易历史 | /trading/history | 历史记录 |
| AI 策略 | /trading/ai | AI 策略生成器 |
| 资产概览 | /wallet | 余额、资产分布 |
| 充值 | /wallet/deposit | 链选择、二维码 |
| 提现 | /wallet/withdraw | 提现表单 |
| API Key | /wallet/api-keys | 交易所绑定 |
| 账单明细 | /wallet/billing | 收支记录 |
| GameFi 概览 | /gamefi | 积分、质押总览 |
| 质押大厅 | /gamefi/staking | A/B 类质押 |
| 积分兑换 | /gamefi/exchange | 标准/急速模式 |
| 释放进度 | /gamefi/vesting | 线性释放 |
| 排行榜 | /gamefi/leaderboard | 积分排名 |
| VPS 实例 | /instances | 实例管理 |
| 设置 | /settings | 账户设置 |
| 安全设置 | /settings/security | 2FA、登录日志 |
| 紧急按钮 | /settings/panic | 一键清仓 |

### 代理商后台 `(agent)`

| 页面 | 路由 | 说明 |
|------|------|------|
| 概览 | /agent | 佣金总览 |
| 推广工具 | /agent/promotion | 链接/海报/邀请码 |
| 业绩报表 | /agent/performance | 返佣曲线 |
| 下级管理 | /agent/referrals | 下级列表 |
| 佣金明细 | /agent/commissions | 佣金记录 |
| 佣金提现 | /agent/withdraw | 佣金提现 |

### 管理后台 `(admin)`

| 页面 | 路由 | 说明 |
|------|------|------|
| 概览 | /admin | 平台数据 |
| 用户管理 | /admin/users | 封号/重置 |
| VPS 监控 | /admin/instances | 全网状态 |
| 财务审计 | /admin/finance | 收支报表 |
| 提现审核 | /admin/finance/withdrawals | 审核队列 |
| 策略管理 | /admin/strategies | 策略上传 |
| 公告管理 | /admin/announcements | 公告发布 |
| Kill Switch | /admin/kill-switch | 全网停机 |

---

## 移动端适配

> 详细规范参见 `UI_ARCHITECTURE.md` 第七章

### 响应式断点

| 断点 | 宽度 | 导航方式 |
|------|------|----------|
| Mobile | < 768px | **底部导航** (5 Tab) |
| Tablet | 768-1024px | 底部导航 |
| Desktop | > 1024px | 左侧边栏 |

### 移动端底部导航（5 Tab）

| Tab | 图标 | 路由 |
|-----|------|------|
| 首页 | Home | /dashboard |
| 交易 | TrendingUp | /trading |
| 策略 | Zap | /strategies |
| 资产 | Wallet | /wallet |
| 我的 | User | /settings |
