---
description: 压缩对话上下文，保留关键信息
argument-hint: [可选：自定义压缩指令]
---

# 上下文压缩命令

压缩当前对话历史，释放上下文空间。

## 默认压缩策略

保留：
1. 最近的代码变更
2. 测试结果和错误信息
3. 重要的架构决策
4. 当前待办事项

删除：
- 中间的调试步骤
- 重复的文件读取
- 探索性的搜索结果

## 自定义压缩

$ARGUMENTS

## 使用内置命令

请使用 `/compact` 执行压缩。

如需自定义压缩策略：
```
/compact 只保留最近 3 个任务的信息
```
