{
  "name": "quantfi",
  "version": "0.1.0",
  "private": true,
  "description": "QuantFi Web3 量化 SaaS 平台",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "api:dev": "pnpm --filter @quantfi/api dev",
    "web:dev": "pnpm --filter @quantfi/web dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@10.16.1"
}
