# 质押页面修复验收文档

## 修复内容

### 1. 添加累计收益和可领取收益显示
- ✅ 在质押汇总区域新增"累计收益"卡片
- ✅ 在质押汇总区域新增"可领取收益"卡片（含领取按钮）
- ✅ 在每个质押卡片底部显示收益信息

### 2. 添加"领取收益"按钮
- ✅ 调用 `/api/gamefi/claim` 接口
- ✅ 使用 toast 提示领取成功
- ✅ 显示领取的积分数量
- ✅ 领取后自动刷新数据

### 3. 更新 Stake 接口
```typescript
interface Stake {
  id: string;
  stake_type: string;
  amount: string;
  lock_days: number;
  weight: string;
  unlocks_at: string;
  status: string;
  created_at: string;
  accumulated_reward: string;  // ✅ 新增
  claimable_reward: string;    // ✅ 新增
  early_penalty?: string;      // ✅ 新增
}
```

### 4. 更新质押规则说明
- ✅ 修改为白皮书 v5.0 规则：
  - A 类质押：权重 1.0x，解押扣除 50% 本金
  - B 类质押：锁定期 30-180 天，权重 1.0x - 3.0x
    - 提前解押：扣除累计收益 + 3% 手续费
    - 到期解押：无惩罚

### 5. 替换 alert() 为 toast
- ✅ 质押成功/失败
- ✅ 赎回成功/失败
- ✅ 领取成功/失败
- ✅ 验证错误（金额、余额）

### 6. 添加解押惩罚警告
- ✅ 显示确认弹窗
- ✅ 展示质押详情（类型、金额、累计收益）
- ✅ 明确显示惩罚金额和规则
- ✅ A 类：显示"扣除 50% 本金"
- ✅ B 类提前解押：显示"扣除累计收益 + 3% 手续费"
- ✅ B 类到期解押：显示"已到期，解押无惩罚"

### 7. fetchData 映射更新
- ✅ 添加 `accumulated_reward` 字段映射
- ✅ 添加 `claimable_reward` 字段映射
- ✅ 添加 `early_penalty` 字段映射

## 页面布局

### 质押汇总（4 张卡片）
1. 总质押金额
2. 累计收益（绿色）
3. 可领取收益（蓝色，含领取按钮）
4. 活跃质押数

### 质押卡片
- 上半部分：质押信息（类型、金额、状态、操作按钮）
- 下半部分（如有收益）：累计收益 + 可领取收益

### 解押确认弹窗
- 质押详情（类型、金额、累计收益）
- 惩罚警告（如有）
- 操作按钮（取消、确认解押）

## 验收步骤

### 1. 启动服务
```bash
cd /Users/azu/主QuantFi
docker compose up -d
```

### 2. 打开页面
浏览器访问：http://localhost:3001/gamefi/staking

### 3. 检查页面显示
- [ ] 质押规则文案正确（白皮书 v5.0）
- [ ] 4 张汇总卡片显示正常
- [ ] 如有可领取收益，"领取"按钮可见

### 4. 测试领取收益
- [ ] 点击"领取"按钮
- [ ] 显示 toast 提示成功
- [ ] 可领取收益归零
- [ ] 页面自动刷新

### 5. 测试解押功能
- [ ] 点击质押卡片的"赎回"按钮
- [ ] 弹出确认对话框
- [ ] 显示质押详情和惩罚警告
- [ ] 点击"确认解押"后显示 toast
- [ ] 页面自动刷新

### 6. 检查边界情况
- [ ] 无收益时不显示收益信息
- [ ] 到期 B 类质押显示"无惩罚"
- [ ] 提前解押显示惩罚金额

## 技术细节

### 使用的组件
- `Dialog` - 解押确认弹窗
- `useToast` - 消息提示
- `Button` - 领取按钮、解押按钮
- `Card` - 汇总卡片、质押卡片

### API 调用
- `gamefiApi.getStakes()` - 获取质押列表
- `gamefiApi.claimRewards()` - 领取收益
- `gamefiApi.unstake(id)` - 赎回质押

### 状态管理
- `claiming` - 领取中状态
- `unstakeDialog` - 解押弹窗状态
- `unstaking` - 赎回中状态

## 代码质量检查

- ✅ TypeScript 编译通过
- ✅ 无硬编码颜色值（使用 CSS 变量）
- ✅ 使用 toast 替代 alert
- ✅ 组件导入正确
- ✅ 接口类型定义完整

## 注意事项

1. **后端字段映射**：目前使用 `accumulated_reward` 同时作为"累计收益"和"可领取收益"，等后端区分后再调整
2. **惩罚金额**：使用后端返回的 `penalty_amount` 字段
3. **领取接口**：使用 `gamefiApi.claimRewards()` 批量领取所有质押的收益

## 回滚方案

如需回滚：
```bash
git checkout apps/web/src/app/(dashboard)/gamefi/staking/page.tsx
```

## 风险评估

- 风险等级：低
- 影响范围：仅质押页面 UI
- 依赖后端：是（需要 `/api/gamefi/claim` 接口）
