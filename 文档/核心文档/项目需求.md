QuantFi: Web3 智能化量化 SaaS 平台终极白皮书
(QuantFi Ultimate White Paper: Architecture, Tokenomics & Risk Control)
版本：v4.0 (Final Release)
密级：核心机密
核心哲学：Web3 内核 (Real Yield) + Web2 外壳 (SaaS) + 单租户隔离 (Security)

第一部分：战略定位与商业全景 (Strategic Overview)
1.1 项目定义
QuantFi 是一个基于开源 Freqtrade 引擎深度构建的 Web3 量化 SaaS 平台。它通过“单租户隔离”技术解决小白用户的量化门槛问题，并结合 GameFi 代币经济模型，将单纯的“付费工具”转化为具备“资产增值”属性的生态系统。
1.2 商业模式 (Business Model)
采用 Real Yield (真实收益) 飞轮模型：
1. SaaS 订阅 (Cash Flow)：用户支付月费（如 $25/月），覆盖 VPS 成本（约 $5/月），剩余为刚性利润。
2. 燃油费/抽成 (Gas Fee)：从用户盈利中抽取 20%（以预充值点卡形式扣除）。这是平台核心利润来源。
3. 资产增值 (Asset Value)：平台收入的 40% 用于回购销毁或分红，推高 $QFI 代币价值。
1.3 目标形态 (Product Form)
* Web 端 / PWA：全功能大本营（策略、资金、质押、后台）。
* Telegram Mini App：裂变与轻量化入口（查看收益、签到、邀请）。
* App Store/Google Play (特洛伊木马策略)：阉割版“数据看板”，仅做行情与状态监控，引流至 Web 端成交，规避 30% 税与合规风险。

第二部分：技术架构与底层设计 (Technical Architecture)
2.1 物理架构：单租户隔离 (Single-Tenant Isolation)
* 核心原则：1 VIP 用户 = 1 独立 DigitalOcean Droplet (VPS)。
* 管理中枢 (Master)：运行 Web/API/DB/Redis。
* 工作节点 (Workers)：
    * 动态编排：通过 Cloud-Init (User Data) 脚本实现全自动开机、装 Docker、跑策略。
    * 生命周期：用户付费即创建，欠费/停止即销毁 (Destroy) 以节省成本。
    * IP 独立：每位用户拥有独立公网 IP，彻底规避交易所连坐封禁风险。
* 数据灾备：
    * 持久化：VPS 销毁前，自动打包 trades.sqlite 和 config.json 上传至 S3 对象存储。
    * 恢复：新 VPS 启动时自动下载恢复，实现“无感迁移”。
2.2 核心引擎 (Core Engine)
* 内核：Freqtrade (Python) 容器化运行。
* AI 赋能：
    * AI 策略向导：前端集成 LLM，支持自然语言生成策略代码（如“RSI<30买入”）。
    * 情绪分析：(二期) 爬取社媒数据生成多空指数供策略调用。
2.3 安全堡垒 (Security Fortress)
* API Key 托管：
    * AES-256-GCM 加密存储于数据库。
    * 仅在 Docker 启动瞬间注入内存环境变量，严禁明文落地。
* 网络风控：VPS 仅开放 SSH（白名单 IP）和 API 端口（内网/鉴权），拒绝公网扫描。

第三部分：代币经济模型 (Tokenomics - The Real Yield)
3.1 双资产体系
1. Q-Points (积分/链下)：无限增发，用于抵扣订阅费、兑换代币。获取方式：交易挖矿、返佣。
2. $QFI (代币/链上)：ERC-20 标准，总量 1 亿枚，通缩模型。
3.2 收入分配飞轮
平台总收入（USDT）分配：
* 40%：运营成本（服务器、API、人力）。
* 40%：回购奖励池（智能合约自动在 DEX 回购 $QFI）。
    * 50% -> 销毁 (Burn)。
    * 50% -> 分发给质押者 (Yield)。
* 20%：风险储备金（冷钱包托底）。
3.3 质押系统 (Dual-Track Staking)
引入 veToken 权重模型：
质押类型	资金来源	提前解押惩罚 (Penalty)	权重乘数
A 类：空投/积分	活动赠送/挖矿	扣除 50% 本金 (销毁)	1.0x
B 类：本金购买	USDT 购买	不扣本金，仅扣累计收益 + 3% 手续费	1.0x - 3.0x (随时间递增)
3.4 释放与兑换 (Vesting)
* 基于订单隔离：每笔兑换独立计算释放期。
* 标准模式：20% 立即释放 + 80% 在 90 天内线性释放。
* 急速模式：立即获得 50%，剩余 50% 直接销毁，订单结束。

第四部分：全维度功能清单 (Detailed Feature List)
4.1 普通用户端 (Web App)
A. 仪表盘 (Dashboard)
* 资产概览（U本位）、今日盈利、VPS 运行状态监控 (CPU/RAM)、公告跑马灯。
B. 量化控制台 (Trading Console)
* 策略中心：官方策略库（一键应用）、AI 策略生成器、参数自定义（杠杆/止损）。
* 回测系统 (Backtesting) [关键补全]：
    * 配置时间/资金/策略 -> 生成资金曲线图、最大回撤、胜率报表。
* 实盘操作：一键启动/停止/重启/紧急平仓。
* 数据同步：WebSocket 实时日志流、历史交易记录明细（开平仓价/盈亏）。
C. 资产钱包 (Wallet)
* USDT 充值/提现（2FA验证）、点卡划转、账单明细（精确到每笔抽成）。
D. GameFi 中心
* 积分兑换（释放进度条）、质押大厅（存币/选周期/看权重/领奖励）。
4.2 代理商后台 (Agent Dashboard)
* 推广工具：专属链接/海报/邀请码。
* 业绩报表：下级列表（脱敏ID/状态/贡献）、实时返佣明细、返佣曲线图。
* 独立提现：代理商佣金独立提现通道。
4.3 总管理后台 (Super Admin)
* 上帝视角：用户管理（封号/重置Key）、VPS 全网监控（一键停机）。
* 财务审计：提现审核（人工核对Hash）、每日平台收支报表。
* 策略管理：上传/更新官方策略文件。

第五部分：全流程风控与容错机制 (Risk Management)
5.1 开发阶段风控 (Dev Phase)
* 沙盒模拟：所有涉及 DigitalOcean 扣费的操作，必须先在 Mock 环境跑通。
* 强类型约束：后端 Pydantic + 前端 TypeScript，杜绝数据结构错误。
* 原子化任务：遵循 CLAUDE.md，每一步必须有验算脚本（如：结算公式 输家-手续费=赢家）。
5.2 运行阶段风控 (Runtime Phase)
* 计费幂等性：数据库 billing_logs 设置 unique_order_id 索引，物理杜绝重复扣费。
* 双向心跳熔断：主控服 15 分钟未收到 VPS 心跳 -> 判定僵尸节点 -> 强制销毁 -> 退款 -> 报警。
* Docker 自愈：配置 restart: always 及内部 Watchdog 脚本，进程死锁自动重启。
* 极端行情保护 (Kill Switch)：管理员后台红色按钮 -> 全网并发调用 /stopbuy -> 停止开单/紧急平仓。
5.3 业务与法律风控 (Legal & Business)
* 文案伪装：
    * 分红 -> 协议收入 (Protocol Revenue)
    * 代客理财 -> 技术服务费 (Gas Fee)
* 用户协议 (ToS)：注册必勾选“本平台仅提供工具，不承担策略运行风险”。
* 反作弊：同 IP/设备指纹关联账号限制，防止点卡对敲套利。

第六部分：开发路线图 (Development Roadmap)
总周期：1.5 - 2 个月 (饱和式开发)
1. Phase 1: 地基与云端验证 (Week 1-2)
    * 数据库 Schema 设计。
    * Cloud-Init 脚本攻坚：跑通“API 开机 -> 自动运行 Freqtrade”全链路。
    * S3 备份恢复系统。
2. Phase 2: SaaS 业务闭环 (Week 3-4)
    * 用户/充值/计费监工脚本。
    * 前端 Web 基础控制台 + 回测系统实现。
3. Phase 3: GameFi 与 代理系统 (Week 5-6)
    * 积分/代币/质押双轨制逻辑。
    * 代理商后台与返佣结算。
    * UI 美化与 TG 适配。
4. Phase 4: 测试与上线 (Week 7-8)
    * 压力测试、DevLog 编写。
    * 特洛伊木马 App 提审。
    * TG 裂变启动。
Freqtrade 原生的 FreqAI (机器学习预测价格) 和 ChatGPT (生成式 AI) 是两回事。我们可以把它们结合起来：
应用场景 A：智能策略编写 (ChatGPT 模式)
* 痛点：用户不会写 Python 代码。
* 功能：用户在网页输入：“我想做一个基于 RSI 的策略，当 RSI 小于 30 买入，大于 70 卖出。”
* AI 后台：调用类似 ChatGPT 的 API，自动生成 UserStrategy.py 代码，并自动部署到用户的机器人里。
* 卖点：“一句话生成你的专属量化机器人”。
应用场景 B：智能投顾/财报解读
* 功能：当机器人亏损或盈利时，AI 生成一段“人话”解释。
* 话术：“老板，刚才亏了 2U，主要是因为比特币突然跳水，但我触发了移动止损，帮你躲过了后面更大的跌幅。”
* 价值：提供极强的情绪价值。

🗺️ 你的 SaaS 演进路线图 (建议)
基于你现在的进度，我为你规划了三个阶段：
* 阶段 1 (当前 - MVP)：
    * 核心：你提供 3-5 个预设好的“黄金策略”（如优化后的 AverageStrategy, NFI）。
    * 用户操作：傻瓜式一键启动，只能改投资金额，不能改参数。
    * AI 含量：无。
* 阶段 2 (进阶版)：
    * 核心：开放“参数微调”和“网页端回测”。
    * AI 含量：引入 ChatGPT 解释交易日志（情绪价值）。
* 阶段 3 (终极形态)：
    * 核心：AI 生成策略代码。
    * AI 含量：用户说话 -> AI 写代码 -> 后台自动回测 -> 实盘运行。

第七部分：UI/UX 设计蓝图 (UI/UX Design Blueprint)

7.1 核心设计理念 (Design Philosophy)
* 风格关键词：极简 (Minimalist)、信任感 (Trustworthy)、暗黑模式 (Dark Mode)。
* 核心逻辑："隐藏复杂，展示收益"。
* ❌ 不要让用户看：config.json、代码、复杂的 K 线分析。
* ✅ 只让用户看：今日收益、一键启动、风险等级选择。
* 参考产品：Robinhood、币安理财、Coinbase、Pionex (派网)、TraderWagon。

7.2 视觉规范 (Visual Identity)
配色方案 (华尔街风格)：
* 背景色：#0B0E11 (深黑蓝，参考币安/Coinbase Pro)
* 主色调 (Brand Color)：#3772FF (科技蓝，用于按钮)
* 涨/盈利：#00C087 (翡翠绿，不要用刺眼的亮绿)
* 跌/亏损：#F23645 (玫瑰红)
* 字体：Inter 或 Roboto (数字显示要清晰)

7.3 页面架构清单 (Site Map)

页面 1: 仪表盘 (Dashboard) —— 用户的"驾驶舱"
用户登录后看到的第一个页面，决定了他是否愿意充值。

A. 顶部核心卡片 (Hero Section)：
   * 总资产 (Total Balance)：显示 USDT 余额 (大号字体)
   * 今日盈亏 (Today's PnL)：用绿色(涨)或红色(跌)显示，例如 `+$12.50 (+1.2%)`
   * 累计收益：展示一条平滑的收益曲线图（只画钱的曲线，不画复杂的 K 线）

B. 机器人状态栏 (Bot Status)：
   * 显示当前运行的策略：`稳健型现货策略 (运行中 🟢)`
   * 核心按钮：一个巨大的 [ 停止 ] / [ 启动 ] 按钮

C. 持仓列表 (Active Positions)：
   * 简单列出：`SOL/USDT - 浮盈 +3%`，`ETH/USDT - 浮盈 -0.5%`
   * 不要显示复杂的订单 ID，只显示币种和赚了多少

页面 2: 策略市场 (Strategy Marketplace) —— 核心卖点
用户在这里挑选"武器"。

A. 卡片式布局 (Card Layout)：
   * 卡片 A：稳健防御型 (AverageStrategy)
     - 标签：`适合熊市` `无回撤` `现货`
     - 数据：`历史年化：15%` `最大回撤：12%`
     - 操作：[ 使用此策略 ]

   * 卡片 B：激进复利型 (未来的合约策略)
     - 标签：`高风险` `带杠杆` `多空双吃`
     - 数据：`历史年化：50%+`
     - 操作：[ 解锁 Pro ] (引导付费)

B. 配置弹窗 (Simple Config)：
   * 用户点击策略后，只允许他填一个数字：[ 投入金额 ]
   * 高级选项折叠起来：止损比例（默认填好优化的 -5.2%，用户可微调）

页面 3: 资产与充值 (Wallet & Billing)
* 充值入口：显示用户的 USDT (TRC20/ERC20) 充值地址
* API 绑定：
  - 如果是用户用自己的币安账号：提供输入 API Key 和 Secret Key 的表单，并附带教程链接
  - UI 细节：输入框加密显示 `******`

页面 4: 设置与风控 (Settings & Safety)
* 黑名单管理：一个简单的多选框，让用户勾选不想交易的币（如 TRX, LUNA）
* 紧急按钮：[ 一键清仓 (Panic Sell) ] —— 遇到黑天鹅时，用户可以点这个按钮把所有币换成 USDT

7.4 用户端完整页面清单

普通用户端：
| 页面 | 路由 | 说明 |
|------|------|------|
| 登录 | /login | 邮箱+密码 |
| 注册 | /register | 邮箱+密码+邀请码 |
| 仪表盘 | /dashboard | 资产概览、收益曲线、机器人状态 |
| 策略市场 | /strategies | 策略卡片、一键启用 |
| 钱包 | /wallet | 充值/提现/API绑定/明细 |
| 交易控制台 | /trading | 回测/实盘/日志 |
| GameFi | /gamefi | 积分/质押/兑换 |
| 设置 | /settings | 黑名单/风控/紧急按钮 |

代理商后台：
| 页面 | 路由 | 说明 |
|------|------|------|
| 推广 | /agent/promotion | 链接/海报/邀请码 |
| 业绩 | /agent/performance | 下级/返佣 |

管理后台：
| 页面 | 路由 | 说明 |
|------|------|------|
| 用户管理 | /admin/users | 封号/重置 |
| VPS 监控 | /admin/instances | 状态/一键停机 |
| 财务审计 | /admin/finance | 提现审核 |

7.5 关键 UI 组件规范

A. 资产展示卡片：
   * 总资产使用 48px 大号字体
   * 盈亏百分比使用 24px，绿涨红跌
   * 卡片圆角 16px，背景 rgba(255,255,255,0.05)

B. 策略卡片：
   * 标签使用小圆角药丸样式
   * 数据使用表格对齐
   * 按钮全宽，高度 48px

C. 收益曲线图：
   * 使用 Recharts 库
   * 只展示资金曲线，不展示 K 线
   * 悬浮显示具体日期和金额

D. 状态指示器：
   * 运行中：绿色圆点 + "运行中"
   * 已停止：灰色圆点 + "已停止"
   * 异常：红色圆点 + "异常"

7.6 响应式断点

| 断点 | 宽度 | 布局 |
|------|------|------|
| Mobile | < 768px | 单列，底部导航 |
| Tablet | 768px - 1024px | 双列 |
| Desktop | > 1024px | 侧边栏 + 主内容区 |
