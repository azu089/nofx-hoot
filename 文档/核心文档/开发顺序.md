# QuantFi v6.2 开发流程

> 基于项目需求 v6.2，按阶段逐步实现

---

## 开发阶段总览

```
┌─────────────────────────────────────────────────────────────┐
│                    开发阶段路线图 (核心优先)                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Phase 1: 环境准备 ✅                                        │
│  └── 初始化项目、安装依赖、配置开发环境                      │
│                                                             │
│  Phase 2: 前端 UI 设计 (3-5天)                               │
│  └── 页面原型、组件库搭建、静态页面                          │
│                                                             │
│  ═══════════════════════════════════════════════════════    │
│  ║ Phase 3: 核心交易系统 MVP (7-10天) ★ 最高优先级 ★       ║    │
│  ║ └── 目标：2个用户同时运行策略 + 执行真实交易            ║    │
│  ║ └── 包含：认证、API Key、策略订阅、信号分发、交易执行  ║    │
│  ═══════════════════════════════════════════════════════    │
│                                                             │
│  Phase 4: 钱包与资金 (3-5天)                                 │
│  └── 充值提现、交易历史、余额管理                            │
│                                                             │
│  Phase 5: 辅助功能 (3-5天)                                   │
│  └── TG 机器人、代理商系统、管理后台                         │
│                                                             │
│  Phase 6: 代币经济 (3-5天)                                   │
│  └── QFI 合约、质押分红、链上交互                            │
│                                                             │
│  Phase 7: 测试与部署 (3-5天)                                 │
│  └── E2E 测试、Docker 部署、监控告警                         │
│                                                             │
│  ═══════════════════════════════════════════════════════    │
│  ║ Phase 8: 第二阶段 - AI Trading + DEX 扩展 🔥 进行中    ║    │
│  ║ └── 8.0 AI 智能交易（双产品：研究团队 + 自动交易）     ║    │
│  ║ └── 8.1 DEX 接入（Hyperliquid/Aster/Lighter）         ║    │
│  ║ └── 8.2 生态中心扩展（质押增强/治理/NFT）              ║    │
│  ═══════════════════════════════════════════════════════    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Phase 1: 环境准备 (1-2天) ✅ 完成

> **⚠️ 依赖安装方式区分**（详见 项目需求.md 0.2.7 节）
>
> | 方式 | 说明 | 能否改源码 |
> |------|------|-----------|
> | `pnpm add` | npm 包（绝大多数） | ❌ 通过 API/配置扩展 |
> | `git clone` | 克隆源码（Freqtrade） | ✅ 改配置和策略 |
> | `复制源码` | 复制到本地（Synthetix） | ✅ 可修改 |
> | `外部服务` | 服务器部署（Coolify） | - |

### 1.1 项目初始化 ✅

```bash
# 创建 monorepo 结构
mkdir -p apps/{web,api,admin,telegram-bot}
mkdir -p packages/{shared,ui}

# 初始化 pnpm workspace
pnpm init
```

**pnpm-workspace.yaml**
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

### 1.2 前端项目创建 ✅ | 业务依赖 Phase 2 安装

| 包名 | 版本 | 用途 | 安装命令 |
|------|------|------|---------|
| Next.js | 14.x | 前端框架 | `pnpm create next-app apps/web --typescript --tailwind --eslint --app` |
| shadcn/ui | latest | UI 组件库 | `npx shadcn@latest init` |
| Tremor | 3.x | 数据可视化 | `pnpm add @tremor/react` |
| **Recharts** | **2.x** | **收益曲线/饼图** | `pnpm add recharts` |
| TradingView LC | 4.x | K线图表 | `pnpm add lightweight-charts` |
| TanStack Query | 5.x | 数据请求 | `pnpm add @tanstack/react-query` |
| TanStack Table | 8.x | 表格 | `pnpm add @tanstack/react-table` |
| React Hook Form | 7.x | 表单 | `pnpm add react-hook-form` |
| Zod | 3.x | 校验 | `pnpm add zod @hookform/resolvers` |
| wagmi | 2.x | Web3 钱包 | `pnpm add wagmi viem` |
| RainbowKit | 2.x | 钱包 UI | `pnpm add @rainbow-me/rainbowkit` |
| **NextAuth.js** | **5.x** | **前端认证** | `pnpm add next-auth@beta` |
| Zustand | 4.x | 状态管理 | `pnpm add zustand` |
| Lucide React | latest | 图标 | `pnpm add lucide-react` |
| date-fns | latest | 日期处理 | `pnpm add date-fns` |
| Socket.io Client | 4.x | 实时通信 | `pnpm add socket.io-client` |

### 1.3 后端项目创建 ✅ | 业务依赖 Phase 3 安装

| 包名 | 版本 | 用途 | 安装命令 |
|------|------|------|---------|
| NestJS | 10.x | 后端框架 | `nest new apps/api` |
| Prisma | 5.x | ORM | `pnpm add prisma @prisma/client` |
| BullMQ | 5.x | 任务队列 | `pnpm add bullmq @nestjs/bullmq` |
| CCXT | 4.x | 交易所 API | `pnpm add ccxt` |
| Socket.io | 4.x | WebSocket | `pnpm add @nestjs/websockets @nestjs/platform-socket.io` |
| **Novu** | **latest** | **多渠道通知** | `pnpm add @novu/node` |
| Passport | latest | 认证 | `pnpm add @nestjs/passport passport passport-jwt passport-local` |
| class-validator | latest | 校验 | `pnpm add class-validator class-transformer` |
| bcrypt | latest | 密码加密 | `pnpm add bcrypt` |
| decimal.js | latest | 精确计算 | `pnpm add decimal.js` |
| ioredis | 5.x | Redis 客户端 | `pnpm add ioredis` |

### 1.4 管理后台创建 ✅ | 依赖已安装 ✅

| 包名 | 版本 | 用途 | 安装命令 |
|------|------|------|---------|
| Refine | 4.x | Admin 框架 | `npm create refine-app@latest apps/admin` |
| Ant Design | 5.x | UI 组件 | 随 Refine 安装 |

### 1.5 区块链依赖 ✅ (contracts/ 已有)

| 包名 | 版本 | 用途 | 安装命令 |
|------|------|------|---------|
| Hardhat | 2.x | 合约开发 | `pnpm add -D hardhat` |
| OpenZeppelin | 5.x | 合约库 | `pnpm add @openzeppelin/contracts` |
| ethers | 6.x | 链上交互 | `pnpm add ethers` |

**参考合约：**

| 合约 | 来源 | 用途 |
|------|------|------|
| **Synthetix StakingRewards** | [GitHub](https://github.com/Synthetixio/synthetix/blob/develop/contracts/StakingRewards.sol) | 质押分红合约参考实现 |

> 注：Synthetix StakingRewards 不是 npm 包，而是参考代码。需要复制到 `contracts/` 目录并根据需求修改。

### 1.6 TG 机器人创建 ✅ | 依赖已安装 ✅

| 包名 | 版本 | 用途 | 安装命令 |
|------|------|------|---------|
| grammY | latest | TG Bot 框架 | `pnpm add grammy` |

### 1.7 开发工具 ✅ (TypeScript/ESLint 随框架安装)

| 包名 | 用途 | 安装命令 |
|------|------|---------|
| TypeScript | 类型检查 | 随框架安装 |
| ESLint | 代码检查 | 随框架安装 |
| Prettier | 代码格式化 | `pnpm add -D prettier` |
| Husky | Git Hooks | `pnpm add -D husky` |

### 1.8 开源项目下载 - Freqtrade (核心信号引擎) ✅

> **Freqtrade** 是 QuantFi 的信号生成引擎，运行在 `dry_run` 模式生成交易信号，由 NestJS 后端分发给用户

**项目地址**: https://github.com/freqtrade/freqtrade

**下载方式（源码下载）**:

```bash
# 1. 克隆 Freqtrade 源码到项目根目录
git clone https://github.com/freqtrade/freqtrade.git freqtrade

# 2. 进入 Freqtrade 目录
cd freqtrade

# 3. 安装 Python 依赖（需要 Python 3.10+）
python -m pip install --upgrade pip
pip install -e .

# 4. 创建用户数据目录
mkdir -p user_data/{strategies,backtest_results,logs,data}

# 5. 生成默认配置
freqtrade new-config --config user_data/config.json

# 6. 下载示例策略
freqtrade new-strategy --strategy SampleStrategy --strategy-path user_data/strategies

# 7. 回到项目根目录
cd ..
```

**Python 环境要求**:

| 依赖 | 版本 | 说明 |
|------|------|------|
| Python | 3.10+ | Freqtrade 运行时 |
| pip | latest | 包管理 |
| TA-Lib | latest | 技术指标库（可选但推荐） |

**关键配置 (freqtrade/user_data/config.json)**:

```json
{
  "dry_run": true,
  "dry_run_wallet": 10000,
  "trading_mode": "futures",
  "margin_mode": "isolated",
  "stake_currency": "USDT",
  "stake_amount": "unlimited",
  "exchange": {
    "name": "binance",
    "key": "",
    "secret": ""
  },
  "api_server": {
    "enabled": true,
    "listen_ip_address": "0.0.0.0",
    "listen_port": 8080,
    "jwt_secret_key": "your-jwt-secret",
    "CORS_origins": ["http://localhost:3001", "http://localhost:4001"]
  },
  "webhook": {
    "enabled": true,
    "url": "http://localhost:4001/api/signals/webhook",
    "webhookbuy": {
      "strategy": "{strategy}",
      "symbol": "{pair}",
      "side": "buy",
      "price": "{rate}"
    },
    "webhooksell": {
      "strategy": "{strategy}",
      "symbol": "{pair}",
      "side": "sell",
      "price": "{rate}"
    }
  }
}
```

**目录结构**:

```
主QuantFi/
├── freqtrade/                    # Freqtrade 源码（git clone）
│   ├── freqtrade/                # 核心代码
│   ├── user_data/                # 用户数据（自定义配置和策略）
│   │   ├── config.json           # 配置文件
│   │   ├── strategies/           # 策略文件
│   │   │   └── SampleStrategy.py
│   │   ├── backtest_results/     # 回测结果
│   │   ├── data/                 # K线数据缓存
│   │   └── logs/                 # 运行日志
│   ├── requirements.txt
│   └── setup.py
```

**启动 Freqtrade (开发模式)**:

```bash
# 在 freqtrade 目录下运行
cd freqtrade
freqtrade trade --config user_data/config.json --strategy SampleStrategy

# 或者启动 API 服务
freqtrade webserver --config user_data/config.json
```

### 1.9 Docker 配置 ✅ | 服务启动 ✅

**docker-compose.yml**
```yaml
services:
  # === 数据库 ===
  postgres:
    image: postgres:15
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: quantfi
      POSTGRES_PASSWORD: quantfi123
      POSTGRES_DB: quantfi
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # === 缓存/队列 ===
  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  postgres_data:
```

> 注：Freqtrade 使用源码方式运行，不在 Docker 中

### 1.10 环境变量模板 ✅

> **详细文档**: 参见 `apps/api/ENV_VARS.md`

**.env.example**
```env
# Database
DATABASE_URL="postgresql://quantfi:quantfi123@localhost:5433/quantfi"

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET="your-jwt-secret"

# API Key 加密密钥 (AES-256-GCM)
ENCRYPTION_KEY="your-32-char-encryption-key"

# Webhook 签名密钥 (HMAC-SHA256)
# 开发环境可留空（跳过验证），生产环境必须配置
WEBHOOK_SECRET="your-webhook-secret"

# API
API_PORT=4001
WEB_PORT=3001

# 运行环境
NODE_ENV=development

# Telegram
TELEGRAM_BOT_TOKEN="your-bot-token"

# Web3
NEXT_PUBLIC_WALLETCONNECT_ID="your-walletconnect-id"

# Exchange (测试用)
BINANCE_API_KEY=""
BINANCE_SECRET=""

# Freqtrade 信号引擎
FREQTRADE_API_URL="http://localhost:8080"
FREQTRADE_JWT_SECRET="your-freqtrade-jwt-secret"
```

**生产环境密钥生成**
```bash
openssl rand -hex 32  # JWT_SECRET, WEBHOOK_SECRET
openssl rand -hex 16  # ENCRYPTION_KEY
```

---

## Phase 2: 前端 UI 设计 (3-5天)

### 2.0 v0.app API 使用规范 ⭐ 强制执行

> **重要**：所有 UI 开发必须使用 v0.app API 生成组件，确保设计一致性和开发效率

#### 2.0.1 API 配置

| 配置项 | 值 |
|--------|-----|
| **API Endpoint** | `https://api.v0.dev/v1/chat/completions` |
| **API Key** | `v1:26xzL1Fetn3agA3ROiaUNYTQ:fBw2Cwh8TiDyiUEL9EKM4thK` |
| **Model** | `v0-1.5-md` |

#### 2.0.2 调用示例

```bash
curl -X POST "https://api.v0.dev/v1/chat/completions" \
  -H "Authorization: Bearer v1:26xzL1Fetn3agA3ROiaUNYTQ:fBw2Cwh8TiDyiUEL9EKM4thK" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "v0-1.5-md",
    "stream": false,
    "messages": [
      {
        "role": "user",
        "content": "生成一个移动端策略市场页面组件..."
      }
    ]
  }'
```

#### 2.0.3 开发规则（强制执行）

| # | 规则 | 说明 |
|---|------|------|
| 1 | **先读桌面端** | 每次生成新页面前，必须先读取对应的桌面端组件了解结构 |
| 2 | **功能对齐** | 移动端必须与桌面端功能完全一致，不允许有差异 |
| 3 | **设计系统一致** | 使用统一的设计系统参数（见下方） |
| 4 | **组件命名规范** | 移动端组件命名为 `mobile-xxx.tsx`，桌面端为 `xxx-v3.tsx` |

#### 2.0.4 设计系统参数（必须提供给 v0）

```
设计系统：
- 背景色: #0A0A0F (主背景)
- 卡片色: #12121A (带 backdrop-blur)
- 边框色: #1E1E2E
- 主强调色: #06B6D4 (cyan)
- 成功色: #10B981 (绿色，用于正收益)
- 危险色: #F43F5E (红色，用于负收益)
- 文字主色: #F8F8FC
- 文字次色: #9090A0
- 文字弱色: #606070

技术栈：
- React 19 + TypeScript
- TailwindCSS (不用 shadcn/ui)
- Lucide React 图标

布局要求：
- 移动端固定顶部标题栏
- 下拉选择用 dropdown，不用横向滚动
- 卡片使用 glass-morphism 效果
```

#### 2.0.5 Prompt 模板

```
请根据以下桌面端组件生成对应的移动端版本：

[粘贴桌面端组件代码]

要求：
1. 功能与桌面端完全一致
2. 移动端优化布局（单列、触摸友好）
3. 固定顶部标题栏
4. 使用以下设计系统：
   - 背景: #0A0A0F, 卡片: #12121A
   - 强调色: #06B6D4 (cyan)
   - 边框: #1E1E2E
5. 使用 lucide-react 图标
6. 导出为单独的 React 组件
```

#### 2.0.6 文件位置

| 类型 | 路径 |
|------|------|
| 桌面端组件 | `apps/web/src/components/ui-v3/{module}/{name}-v3.tsx` |
| 移动端组件 | `apps/web/src/components/ui-v3/mobile/mobile-{name}.tsx` |
| 预览页面 | `apps/web/src/app/preview/page.tsx` |

---

### 2.1 目标

- 完成所有页面的静态 UI
- 建立可复用的组件库
- 确保移动端适配
- **使用 v0.app API 生成所有 UI 组件**

### 2.2 页面清单与优先级

| 优先级 | 页面 | 路由 | 状态 |
|--------|------|------|------|
| **P0** | 登录 | /login | [ ] |
| **P0** | 注册 | /register | [ ] |
| **P0** | 仪表盘 | /dashboard | [ ] |
| **P0** | 策略市场 | /strategies | [ ] |
| **P0** | 策略详情 | /strategies/[id] | [ ] |
| **P0** | 持仓管理 | /trading | [ ] |
| **P0** | 钱包 | /wallet | [ ] |
| **P1** | 充值 | /wallet/deposit | [ ] |
| **P1** | 提现 | /wallet/withdraw | [ ] |
| **P1** | API Key | /wallet/api-keys | [ ] |
| **P1** | 质押 | /staking | [ ] |
| **P1** | 设置 | /settings | [ ] |
| **P2** | 回测 | /trading/backtest | [ ] |
| **P2** | 交易历史 | /trading/history | [ ] |
| **P2** | Landing | / | [ ] |

### 2.3 组件库搭建

**基础 UI 组件 (shadcn/ui)**

```bash
# 安装常用组件
npx shadcn@latest add button card dialog input label select \
  table tabs toast dropdown-menu avatar badge skeleton \
  sheet sidebar navigation-menu form
```

**业务组件清单**

| 组件 | 位置 | 用途 |
|------|------|------|
| `AssetCard` | components/features/dashboard | 资产卡片 |
| `ProfitChart` | components/charts | 收益曲线 (Tremor) |
| `TradingChart` | components/charts | K线图 (TradingView LC) |
| `StrategyCard` | components/features/strategies | 策略卡片 |
| `PositionCard` | components/features/trading | 持仓卡片 |
| `WalletBalance` | components/features/wallet | 余额展示 |
| `MobileNav` | components/layout | 移动端底部导航 |
| `Sidebar` | components/layout | 桌面端侧边栏 |

### 2.4 样式系统

**globals.css 配色**

```css
:root {
  /* 背景 */
  --bg-primary: #0B0E11;
  --bg-secondary: #131722;
  --bg-tertiary: #1E222D;

  /* 品牌色 */
  --brand-primary: #3772FF;
  --brand-secondary: #2962FF;

  /* 语义色 */
  --success: #00C087;
  --danger: #F23645;
  --warning: #F7931A;

  /* 文字 */
  --text-primary: #FFFFFF;
  --text-secondary: #848E9C;
  --text-tertiary: #5E6673;

  /* 边框 */
  --border-primary: #2B3139;
}
```

### 2.5 布局结构

```
apps/web/src/
├── app/
│   ├── (public)/           # 无需登录
│   │   ├── layout.tsx      # 简洁布局
│   │   ├── page.tsx        # Landing
│   │   ├── login/
│   │   └── register/
│   │
│   ├── (dashboard)/        # 需登录
│   │   ├── layout.tsx      # Sidebar + MobileNav
│   │   ├── dashboard/
│   │   ├── strategies/
│   │   ├── trading/
│   │   ├── wallet/
│   │   ├── staking/
│   │   └── settings/
│   │
│   └── providers.tsx       # TanStack Query + wagmi
│
├── components/
│   ├── ui/                 # shadcn/ui
│   ├── layout/             # Sidebar, MobileNav
│   ├── charts/             # Tremor, TradingView
│   └── features/           # 业务组件
│
└── lib/
    ├── api.ts              # API 客户端
    └── wagmi.ts            # Web3 配置
```

---

## Phase 3: 核心交易系统 MVP (7-10天) ★ 最高优先级

> **目标**：让 2 个用户同时运行策略并执行真实交易
> **验收标准**：用户 A 和用户 B 各自绑定 API Key，订阅同一策略，Freqtrade 发出信号后，两人同时收到并执行交易

### 3.1 MVP 数据库设计 (最小表结构)

```prisma
// ========== MVP 必需表 ==========

// 用户 (最小字段)
model User {
  id            String   @id @default(uuid())
  email         String   @unique
  password      String
  nickname      String?

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  // 关系
  apiKeys       ApiKey[]
  positions     Position[]
  subscriptions StrategySubscription[]
}

// 交易所 API Key (核心)
model ApiKey {
  id            String   @id @default(uuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])

  exchange      String   // binance, okx, bybit
  label         String

  // 加密存储 (AES-256-GCM)
  encryptedKey    String
  encryptedSecret String
  iv              String
  authTag         String

  isActive      Boolean  @default(true)
  createdAt     DateTime @default(now())
}

// 策略
model Strategy {
  id            String   @id @default(uuid())
  name          String
  description   String
  freqtradeId   String   @unique  // 对应 Freqtrade 策略名

  isActive      Boolean  @default(true)
  createdAt     DateTime @default(now())

  subscriptions StrategySubscription[]
  signals       Signal[]
}

// 策略订阅 (用户 <-> 策略 多对多)
model StrategySubscription {
  id            String   @id @default(uuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])
  strategyId    String
  strategy      Strategy @relation(fields: [strategyId], references: [id])

  // 用户配置
  apiKeyId        String   // 使用哪个 API Key
  amountPerTrade  Decimal  @db.Decimal(18, 8)  // 每单金额
  maxPositions    Int      @default(3)

  isActive      Boolean  @default(true)
  createdAt     DateTime @default(now())

  @@unique([userId, strategyId])
}

// 交易信号 (从 Freqtrade 接收)
model Signal {
  id            String   @id @default(uuid())
  strategyId    String
  strategy      Strategy @relation(fields: [strategyId], references: [id])

  symbol        String   // BTC/USDT
  side          String   // buy, sell
  price         Decimal  @db.Decimal(18, 8)

  // 信号分发追踪
  distributedAt DateTime?

  createdAt     DateTime @default(now())
}

// 持仓
model Position {
  id            String   @id @default(uuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])

  exchange      String
  symbol        String
  side          String   // long, short

  entryPrice    Decimal  @db.Decimal(18, 8)
  amount        Decimal  @db.Decimal(18, 8)

  // 交易所返回的订单ID
  exchangeOrderId String?

  status        String   @default("open") // open, closed, failed
  closedAt      DateTime?
  closePrice    Decimal? @db.Decimal(18, 8)
  pnl           Decimal? @db.Decimal(18, 8)

  signalId      String?

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}
```

### 3.2 MVP 任务清单 (按执行顺序)

#### 3.2.1 后端基础设施 (1-2天) ✅ 完成
- [x] 安装后端依赖 (Prisma, BullMQ, CCXT, Passport)
- [x] 配置 Prisma + 数据库迁移
- [x] 配置 BullMQ + Redis 连接
- [x] 统一响应格式 + 错误处理

#### 3.2.2 用户认证 (1天) ✅ 完成
- [x] POST /auth/register - 用户注册
- [x] POST /auth/login - 用户登录 (JWT)
- [x] GET /auth/me - 获取当前用户
- [x] JWT Guard 中间件

#### 3.2.3 API Key 管理 (1天) ✅ 完成
- [x] POST /api-keys - 添加 API Key (AES-256-GCM 加密)
- [x] GET /api-keys - 获取用户的 API Key 列表
- [x] DELETE /api-keys/:id - 删除 API Key
- [x] 加密/解密工具函数

#### 3.2.4 策略订阅 (1天) ✅ 完成
- [x] GET /strategies - 策略列表
- [x] GET /strategies/:id - 策略详情
- [x] POST /strategies/:id/subscribe - 订阅策略
- [x] DELETE /strategies/:id/subscribe - 取消订阅
- [ ] GET /my/subscriptions - 我的订阅 (待实现)

#### 3.2.5 信号接收与分发 (2天) ⭐ 核心 ✅ 完成
- [x] POST /signals/webhook - 接收 Freqtrade Webhook
- [ ] 信号验证 (签名/来源校验) - 待实现
- [x] BullMQ 信号队列 (signal-queue)
- [x] 信号处理器 (SignalProcessor)
  - 查找订阅该策略的用户
  - 为每个用户创建交易任务
- [x] BullMQ 交易队列 (trade-queue)
- [x] 🆕 信号执行追踪 (SignalExecution)

#### 3.2.6 交易执行 (2天) ⭐ 核心 ✅ 完成

> **架构说明**：Freqtrade 底层用的就是 CCXT，我们不需要复制 Freqtrade 代码，
> 而是**直接利用 CCXT 已有的能力**处理交易细节（精度、最小量、余额等）。

**CCXT 内置能力（直接使用，无需自己实现）**：

| 功能 | CCXT 方法 | 说明 |
|------|-----------|------|
| 数量精度 | `exchange.amountToPrecision(symbol, amount)` | 自动处理小数位 |
| 价格精度 | `exchange.priceToPrecision(symbol, price)` | 自动处理价格精度 |
| 最小下单量 | `exchange.markets[symbol].limits.amount.min` | 获取最小下单量 |
| 余额查询 | `exchange.fetchBalance()` | 获取可用余额 |
| 市场信息 | `exchange.loadMarkets()` | 加载交易对信息 |

**示例代码（利用 CCXT 内置能力）**：

```typescript
// 使用 CCXT 的内置方法处理精度和限制
async function executeOrder(exchange: ccxt.Exchange, symbol: string, side: string, amount: number) {
  // 1. 加载市场信息（CCXT 自动缓存）
  await exchange.loadMarkets();

  // 2. 检查最小下单量（CCXT 提供）
  const minAmount = exchange.markets[symbol].limits.amount.min;
  if (amount < minAmount) {
    throw new Error(`下单量 ${amount} 低于最小值 ${minAmount}`);
  }

  // 3. 精度处理（CCXT 内置方法）
  const preciseAmount = exchange.amountToPrecision(symbol, amount);

  // 4. 创建订单
  const order = await exchange.createMarketOrder(symbol, side, preciseAmount);

  return order;
}
```

**任务清单** ✅ 完成：

- [x] CCXT 交易服务封装
  - 初始化交易所连接 (`new ccxt.binance()`)
  - 使用 `loadMarkets()` 获取市场信息
  - 使用内置精度方法 (`amountToPrecision`, `priceToPrecision`)
  - 使用内置限制检查 (`markets[symbol].limits`)
  - 创建订单 (`createMarketOrder` / `createLimitOrder`)
  - 查询订单状态 (`fetchOrder`)
- [x] 交易处理器 (TradeProcessor)
  - 解密用户 API Key
  - 余额检查 (`fetchBalance`)
  - 执行交易（利用 CCXT 内置能力）
  - 记录持仓
  - 处理失败重试
- [x] 持仓管理
  - GET /positions - 持仓列表
  - POST /positions/:id/close - 手动平仓
  - GET /positions/telegram/:telegramId - TG Bot 查询
- [x] 🆕 风控系统 (RiskControlService)
  - 最大持仓数检查 (maxPositions)
  - 同币种重复持仓检查 (allowSameSymbol)
  - 每日交易次数限制 (maxDailyTrades)
  - 交易所余额检查 (minBalance)
  - 风控拒绝日志记录 (RiskLog)

#### 3.2.7 前端对接 (1-2天) ✅ 完成
- [x] 登录/注册页面对接 API ✅ 2026-01-31 (含钱包登录)
- [x] API Key 管理页面
- [x] 策略订阅页面
- [x] 持仓列表页面
- [x] 实时状态展示

### 3.3 MVP 数据流

```
┌─────────────────────────────────────────────────────────────┐
│                      MVP 核心数据流                          │
│                                                             │
│   架构核心：Freqtrade (信号) → NestJS + CCXT (执行)          │
│   关键点：CCXT 已内置精度处理、最小量检查、余额查询等功能     │
└─────────────────────────────────────────────────────────────┘

  Freqtrade (dry_run 模式)
       │
       │ Webhook (买入/卖出信号)
       │ 只负责策略判断，不做真实交易
       ▼
  ┌─────────────┐
  │ NestJS API  │ POST /signals/webhook
  │  信号接收   │
  └─────┬───────┘
        │
        ▼
  ┌─────────────┐
  │  BullMQ     │ signal-queue
  │  信号队列   │
  └─────┬───────┘
        │
        ▼
  ┌─────────────┐     查询订阅用户
  │ 信号处理器  │ ──────────────────► 用户 A (订阅策略 X)
  │ SignalProc  │ ──────────────────► 用户 B (订阅策略 X)
  └─────┬───────┘
        │
        │ 为每个用户创建交易任务
        ▼
  ┌─────────────┐
  │  BullMQ     │ trade-queue
  │  交易队列   │
  └─────┬───────┘
        │
        ▼
  ┌───────────────────────────────────────────────────────┐
  │ 交易处理器 (TradeProcessor)                            │
  │                                                       │
  │  1. 解密用户 API Key                                   │
  │  2. 使用 CCXT 内置方法：                               │
  │     - fetchBalance()        → 余额检查                │
  │     - markets[symbol].limits → 最小下单量检查          │
  │     - amountToPrecision()   → 数量精度处理            │
  │     - priceToPrecision()    → 价格精度处理            │
  │  3. 执行订单                                          │
  │     - createMarketOrder() / createLimitOrder()        │
  └───────────────────────────────────────────────────────┘
        │
        │ CCXT 统一接口下单
        ▼
  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
  │  Binance    │     │    OKX      │     │   Bybit     │
  │  用户 A     │     │   用户 B    │     │   用户 C    │
  └─────────────┘     └─────────────┘     └─────────────┘
```

### 3.3.1 架构要点

| 组件 | 职责 | 不做什么 |
|------|------|---------|
| **Freqtrade** | 运行策略、生成买卖信号 | 不做真实交易 (dry_run) |
| **NestJS** | 接收信号、用户管理、订阅管理、任务调度 | 不复制 Freqtrade 代码 |
| **CCXT** | 交易执行、精度处理、余额查询、订单管理 | - |
| **BullMQ** | 异步任务队列、失败重试 | - |

> **重要**：所有交易细节（精度、最小量、余额等）由 CCXT 处理，
> 我们不需要自己实现这些逻辑，直接调用 CCXT 内置方法即可。

### 3.4 MVP 验收测试

```bash
# 1. 准备测试用户
# 用户 A: alice@test.com, 绑定 Binance API Key
# 用户 B: bob@test.com, 绑定 OKX API Key

# 2. 两用户都订阅策略 "RSI_Strategy"

# 3. 模拟 Freqtrade 发送信号
curl -X POST http://localhost:4001/api/signals/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "strategy": "RSI_Strategy",
    "symbol": "BTC/USDT",
    "side": "buy",
    "price": "95000"
  }'

# 4. 验证结果
# - 用户 A 在 Binance 开仓成功
# - 用户 B 在 OKX 开仓成功
# - 两人持仓列表都显示新持仓
```

---

## Phase 4: 钱包与资金 (3-5天) ✅ 后端完成

> Phase 3 测试通过后再开发

### 4.1 任务清单

- [x] 用户余额字段 (usdtBalance, hootBalance)
- [x] GET /wallet/balance - 余额查询
- [x] GET /wallet/transactions - 交易记录
- [x] 充值地址生成 (POST /wallet/deposit-address)
- [x] 提现申请 (POST /wallet/withdraw)
- [ ] 链上监听 (待实现)
- [x] 交易手续费计算 (FeeService 已完成)

### 4.2 数据库扩展

```prisma
// 交易记录
model Transaction {
  id            String   @id @default(uuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])

  type          String   // deposit, withdraw, fee
  asset         String   // USDT, QFI
  amount        Decimal  @db.Decimal(18, 8)

  uniqueOrderId String   @unique  // 幂等性
  status        String   @default("pending")
  txHash        String?

  createdAt     DateTime @default(now())
}
```

---

## Phase 5: 辅助功能 (3-5天) ✅ 完成

### 5.1 TG 机器人 ✅ 完成

- [x] 账户绑定 (/bind) - 后端 API: POST /auth/bind-telegram
- [x] 查看状态 (/status) - 后端 API: GET /auth/telegram/:telegramId
- [x] 查看持仓 (/positions) - 后端 API: GET /positions/telegram/:telegramId
- [x] 开仓/平仓通知推送 ✅ 2026-01-31 完成 (NotificationsService + TG Bot HTTP API 集成)

### 5.2 代理商系统 ✅ 完成

- [x] 邀请码生成 (GET /referral/invite-code)
- [x] 邀请关系树 (GET /referral/invitees)
- [x] 返佣计算 (GET /referral/rewards)
- [x] 绑定邀请码 (POST /referral/bind)
- [x] 邀请统计 (GET /referral/stats)

### 5.3 管理后台 (Refine) ✅ 完成

- [x] 用户管理 (GET/PUT /admin/users)
- [x] 策略管理 (GET/POST/PUT/DELETE /admin/strategies)
- [x] 提现审核 (GET /admin/withdraws, POST /admin/withdraws/:id/process)
- [x] 仪表盘统计 (GET /admin/dashboard)
- [x] 质押管理 (GET /admin/staking/stats, POST /admin/staking/dividend-pool)
- [x] 前端 UI 对接 ✅ 2026-01-31 完成 (Refine + Ant Design)

---

## Phase 6: 代币经济 (3-5天) ✅ 后端完成

### 6.1 任务清单

- [ ] HOOT ERC-20 合约 (智能合约待开发)
- [ ] 质押合约 StakingRewards (智能合约待开发)
- [x] 前端钱包连接 wagmi + WalletConnectModal ✅ 2026-01-31 完成
- [x] 质押/解押功能 (POST /staking, POST /staking/:id/unstake)
- [x] 分红计算与发放 (StakingService 完成)
- [x] 🆕 质押权重计算 (1.0x - 3.0x)
- [x] 🆕 分红池管理 (admin/staking/dividend-pool)
- [x] 🆕 用户质押统计 (GET /staking/my/stats)
- [x] 🆕 全网质押统计 (GET /staking/global-stats)

### 6.2 HOOT 代币上链部署策略（安全规范）

> **核心原则：先部署后公告，不留时间差，杜绝恶意抢跑**

⚠️ **风险说明**：如果提前泄露代币名称和上链时间，恶意方可能抢先发行同名代币占位，造成用户混淆和资金损失。

**部署执行顺序（严格按序，不可跳步）：**

```
Step 1: 部署合约
   └── 在主网部署 HOOT ERC-20 合约
   └── 记录合约地址，内部验证

Step 2: 锁定流动性
   └── 添加初始流动性到 DEX（如 PancakeSwap）
   └── 使用流动性锁定工具锁仓（如 Team.Finance / PinkLock）
   └── 确保锁仓时间和金额满足社区信任要求

Step 3: 官网公示合约地址
   └── 在 hoot.cool 官网显眼位置展示合约地址
   └── 提供一键添加代币到钱包的功能
   └── 确保用户能验证官方合约地址

Step 4: 统一公告
   └── TG 官方频道同步发布公告
   └── 包含：合约地址、流动性锁仓证明、购买教程
   └── Bot 推送通知给所有已注册用户
```

**禁止事项：**
- ❌ 禁止在合约部署前公开上链时间或代币名称
- ❌ 禁止在流动性锁定前开放交易
- ❌ 禁止非官方渠道提前泄露合约地址
- ❌ TG Bot / 网站文案中禁止写具体上链时间（如"3个月后"）

**文案规范：**
- ✅ "代币上链计划进行中，请关注官方公告"
- ❌ "代币将在 X 月后上链"

---

## Phase 7: 测试与部署 (3-5天)

### 7.0 扩展后端模块 ✅ 完成

- [x] WebSocket 实时通信 (TradingGateway)
  - 用户连接/断开管理
  - 策略信号订阅
  - 持仓更新推送
  - 系统公告广播
- [x] 通知系统 (NotificationsModule)
  - 多渠道推送 (WebSocket/Telegram/Email)
  - 通知模板
  - 未读计数
- [x] 健康检查 (HealthModule)
  - /health - 完整健康检查
  - /health/liveness - K8s 存活探针
  - /health/readiness - K8s 就绪探针
  - /health/metrics - 系统指标
  - /health/stats - 业务统计
- [x] 信号执行追踪 (SignalExecution)
  - 执行状态追踪 (pending/queued/executing/success/failed/skipped)
  - 执行统计 (/signals/stats)
  - 用户执行历史 (/signals/my/executions)

### 7.1 测试 ✅ 完成

- [x] 单元测试 (Jest) - auth, strategy, staking, api-keys, referral, wallet, fee
- [x] E2E 测试 - auth, wallet, strategy, health
- [ ] 多用户并发测试

### 7.2 部署 ✅ 完成

- [x] Docker 镜像构建 (Dockerfile for API & Web)
- [x] 生产环境配置 (docker-compose.prod.yml)
- [x] 监控告警 (health endpoints + monitor.sh)
- [x] 部署脚本 (scripts/deploy.sh)
- [x] Nginx 反向代理配置

---

## 当前进度

- **当前阶段**: 🔥 **Phase 8.0 AI 智能交易模块开发中**
- **第一阶段完成度**: 100%（Phase 1-7 全部完成）
- **第二阶段进度**: Phase 8.0 方案定稿，准备开始实施
- **最近更新**: 2026-02-10 - AI 模块双产品方案定稿（AI 独立执行层 + 深研研究管线）
- **已完成**:
  - [x] Phase 1 全部完成 - 环境准备
  - [x] Phase 2 前端 UI 设计 **完成**
    - [x] 所有页面组件 (60+ 组件)
    - [x] 移动端适配
    - [x] 玻璃态设计系统
  - [x] Phase 3 核心交易系统 MVP **完成**
    - [x] 用户认证 (Auth Module) + 前端对接
    - [x] API Key 管理 (ApiKeys Module) + 前端对接
    - [x] 策略订阅 (Strategies Module) + 前端对接
    - [x] 信号接收与分发 (Signals Module + BullMQ)
    - [x] 交易执行 (Trading Module + CCXT)
    - [x] 持仓管理 (Positions) + 前端对接
    - [x] 🆕 风控系统 (RiskControlService) - 持仓/余额/频次检查
  - [x] Phase 4 钱包与资金 **完成**
    - [x] 余额管理 (Wallet Module) + 前端对接
    - [x] 交易记录 + 前端对接
    - [x] 充值/提现 + 前端对接
    - [x] 链上监听服务 (BlockchainService) - 配置即启用
  - [x] Phase 5 辅助功能 **完成**
    - [x] TG Bot 完整实现 (命令 + HTTP API + 通知推送) ✅ 2026-01-31
    - [x] 代理商系统 (Referral Module) + 前端对接
    - [x] 管理后台完整对接 (Admin Module + Refine UI) ✅ 2026-01-31
  - [x] Phase 6 代币经济 **完成**
    - [x] 质押系统 (Staking Module) + 前端对接
    - [x] 分红计算与发放
    - [x] Web3 钱包连接 (wagmi + WalletConnectModal) ✅ 2026-01-31
  - [x] Phase 7 测试与部署 **完成**
    - [x] WebSocket 实时通信 (TradingGateway)
    - [x] 通知系统 (Notifications Module)
    - [x] 健康检查 (Health Module)
    - [x] 信号执行追踪 (SignalExecution)
    - [x] Webhook 签名验证 (HMAC-SHA256)
    - [x] 交易手续费计算 (FeeService)
    - [x] 单元测试 (7 个 service 测试文件)
    - [x] E2E 测试 (4 个测试文件)
    - [x] Docker 生产配置 (docker-compose.prod.yml)
    - [x] 部署脚本 (scripts/deploy.sh)
    - [x] 监控脚本 (scripts/monitor.sh)
    - [x] Nginx 反向代理配置
  - [x] 智能合约开发 **完成**
    - [x] HOOT ERC-20 代币合约 (HOOTToken.sol)
    - [x] StakingRewards 质押分红合约
    - [x] 合约部署脚本 (deploy.ts)
    - [x] 合约测试 (54 个测试全部通过)
- **第二阶段 (进行中)**:
  - [ ] Phase 8.0 AI 智能交易模块 🔥 **当前进行中**
    - 产品 A：AI 研究团队（深研模式）
    - 产品 B：AI 自动交易（快速交易模式）
    - AI 独立执行层（核心执行逻辑）
  - [ ] Phase 8.1 DEX 接入
  - [ ] Phase 8.2 生态中心扩展
- **已完成阶段**:
  - 生产环境部署配置
  - Freqtrade 策略交易全链路测试

---

## 开发优先级说明

```
优先级排序：

1. Phase 2: UI 设计
   └── 完成页面原型和静态 UI

2. Phase 3: 核心交易系统 MVP ★★★
   └── 目标：2 个用户同时运行策略 + 执行真实交易
   └── 这是产品的核心价值，必须先验证可行性

3. Phase 4-7: 扩展功能
   └── 核心功能测试通过后再开发
```

---

## Phase 8: 第二阶段开发 - AI Trading + DEX 扩展 (进行中)

> **说明**：第一阶段 (Phase 1-7) 完成后的核心功能扩展
> **开发原则**：⭐ **开源优先** - 优先使用成熟开源方案，避免重复造轮子
> **更新日期**：2026-02-08

### 8.0 AI 智能交易模块 ★★★ 最高优先级

> **目标**：双产品 AI 交易系统 — 研究型 + 自动型
> **来源**：AI 研究管线 + 快速交易执行层
> **核心设计**：AI 交易独立执行（不走 BullMQ 队列）
> **详细产品文档**：`文档/核心文档/AI交易模块-产品形态与开发规划.md`
> **更新日期**：2026-02-10

#### 8.0.0 两个 AI 产品概述

| | 产品 A：AI 研究团队 | 产品 B：AI 自动交易 |
|---|---|---|
| **模式** | 深研模式 | 快速交易模式 |
| **核心能力** | 多智能体 5 阶段深度研究 → 决策 | 策略配置 → 自动循环交易 |
| **触发方式** | 用户手动触发 | 自动周期运行 |
| **决策模式** | 4 分析师 → 多头/空头辩论 → 风控三方辩论 | Solo 单 AI / Debate 多 AI 辩论投票 |
| **交易执行** | 直接调 CCXT（不走队列） | 直接调 CCXT（不走队列） |

#### 8.0.1 关键架构决策：AI 独立执行层

```
⚠️ Freqtrade 策略交易（已有，不改动）：
  Signal → BullMQ 'trade' 队列 → TradeProcessor → CCXT
  （适合信号分发场景，多用户排队处理）

✅ AI 交易（本模块，全新独立）：
  AI 决策 → ai-execution.service → 独立 CCXT 实例 → 交易所
  （与 Freqtrade 完全隔离，避免冲突）
```

**ai-execution.service 核心能力：**

| 功能 | HOOT TypeScript 实现 |
|--------------|---------------------|
| `OpenLong/OpenShort` | 独立 CCXT 实例市价开仓 |
| `CloseLong/CloseShort` | 支持全平和部分平仓 |
| `SetLeverage` + 5 秒冷却期 | 开仓前自动设置杠杆 |
| `SetStopLoss/SetTakeProfit` (Algo Order) | CCXT createOrder + stopLoss/takeProfit 参数 |
| `CancelAllOrders` | 开仓前清除旧挂单 |
| `SetMarginMode` | 逐仓/全仓模式设置 |
| `CheckMinNotional` | 最小名义值检查 (≥10 USDT) |
| `enforceMaxPositions` | 最大持仓数硬限制 |
| `enforcePositionValueRatio` | BTC/ETH vs 山寨币不同仓位比率上限 |
| `enforceMinPositionSize` | 最小仓位检查 |
| 余额自适应（自动缩减仓位 + 2% 缓冲） | 余额不足时自动降低仓位 |
| 决策排序（平仓优先于开仓） | 防止持仓堆积溢出 |
| 订单确认轮询（5 次） | 确保成交后再设 SL/TP |

**共享资源（不重复开发）：**
- API Key 解密 → 复用 `ApiKeysService.getDecryptedApiKey()`
- Position 表 → 复用现有表，通过 `source='ai_research'/'ai_strategy'` 区分
- 燃油费 → 复用 `FeeService.calculateGasFee()`

#### 8.0.2 开源依赖清单

| 库 | 版本 | 用途 | 安装命令 | 开箱即用 |
|----|------|------|----------|---------|
| **openai** | 4.x | LLM API 调用（兼容 OpenAI/DeepSeek/Gemini） | `pnpm add openai` | 95% |
| **technicalindicators** | 3.x | 技术指标计算（RSI/MACD/BB/ATR/OBV） | `pnpm add technicalindicators` | 90% |
| **zod** | 3.x | LLM 结构化输出校验（已安装） | ✅ 已有 | 100% |
| **BullMQ** | 5.x | AI 监控 + 自动调度（已安装） | ✅ 已有 | 100% |
| **CCXT** | 4.x | 市场数据 + 交易执行（已安装） | ✅ 已有 | 100% |
| **decimal.js** | 10.x | 仓位/成本精确计算（已安装） | ✅ 已有 | 100% |

> **关键决策**：使用 `openai` 官方 SDK 的 OpenAI-compatible 模式调用所有模型。
> **不使用** LangChain/LlamaIndex 等重型框架，直接调用 API 更简单可控。

#### 8.0.3 v2-dev 复用清单

| 文件 | 复用方式 | 说明 |
|------|---------|------|
| `services/llm.service.ts` | 直接复用 | LLM 调用（DeepSeek/OpenAI/OpenRouter） |
| `services/debate.service.ts` | 直接复用 | 5 角色辩论引擎 |
| `services/memory.service.ts` | 直接复用 | BM25 记忆系统 |
| `services/evolution.service.ts` | 直接复用 | Sharpe 分层进化 |
| `services/indicators.service.ts` | 直接复用 | 技术指标计算 |
| `services/trade-history.service.ts` | 直接复用 | 交易历史分析 |
| `services/ai-performance.service.ts` | 直接复用 | 性能追踪 |
| `constants/prompts.ts` | 直接复用 | 提示词模板 |
| `services/market-data.service.ts` | 修复后复用 | 修复方法签名 |
| `services/safety.service.ts` | 修复后复用 | 补完 L5 回撤计算 |
| `services/auto-scheduler.service.ts` | 改造复用 | 改为产品 B 专用 |
| `ai.service.ts` | 重写 | 拆分为两个产品编排器 |
| `processors/ai-trade.processor.ts` | 废弃 | 替换为 ai-execution.service |
| 5 个 spec 测试文件 | 直接复用 | debate/evolution/indicators/memory/safety |

#### 8.0.4 后端模块结构

```
apps/api/src/modules/ai/
├── ai.module.ts                       # 双产品模块注册
├── ai.controller.ts                   # 双产品 API 路由
├── dto/
│   ├── research.dto.ts               # 产品 A 入参
│   ├── strategy.dto.ts               # 产品 B 入参
│   └── ai-config.dto.ts              # 通用配置
├── services/
│   │── ── 共享基础（v2-dev 复用）──
│   ├── llm.service.ts                # LLM 调用
│   ├── debate.service.ts             # 辩论引擎
│   ├── memory.service.ts             # BM25 记忆
│   ├── evolution.service.ts          # Sharpe 进化
│   ├── indicators.service.ts         # 技术指标
│   ├── trade-history.service.ts      # 交易历史
│   ├── ai-performance.service.ts     # 性能追踪
│   ├── market-data.service.ts        # 市场数据（修复）
│   ├── safety.service.ts             # 安全检查（补完）
│   │── ── 核心执行 ──
│   ├── ai-execution.service.ts       # AI 独立交易执行
│   │── ── 产品 A：AI 研究团队（深研模式）──
│   ├── research-pipeline.service.ts  # 5 阶段流水线编排
│   ├── crypto-analysts.service.ts    # 4 类加密货币分析师
│   ├── risk-debate.service.ts        # 风控三方辩论
│   ├── research-reflection.service.ts # 交易后反思
│   │── ── 产品 B：AI 自动交易（快速交易模式）──
│   ├── strategy-engine.service.ts    # 策略配置 CRUD + 管理
│   ├── auto-trader.service.ts        # 自动交易循环
│   ├── coin-scanner.service.ts       # 币种扫描（static/ai/oi_top）
│   ├── consensus.service.ts          # 多 AI 加权投票共识
│   └── grid-trading.service.ts       # Grid 网格交易
├── constants/
│   └── prompts.ts                    # 提示词模板（复用 + 扩展）
└── processors/
    ├── drawdown-monitor.processor.ts # AI 持仓回撤监控
    └── auto-run.processor.ts         # 产品 B 自动运行调度
```

#### 8.0.5 数据库扩展

**扩展已有表：**
- `AiAnalysis` 新增 `product`（'research'/'strategy'）、`sessionId` 字段
- `AiConfig` 保持现有字段不变

**新增表：**

```prisma
// 产品 A：研究会话
model AiResearchSession {
  id              String   @id @default(uuid())
  userId          String   @map("user_id")
  symbol          String                        // BTC/USDT
  depth           String   @default("quick")    // quick/standard/deep
  autoExecute     Boolean  @default(false)      // 研究后自动执行
  status          String   @default("running")  // running/completed/failed
  currentStage    Int      @default(1)          // 当前阶段 1-5
  stages          Json     @default("{}")       // 5 阶段详细内容
  finalDecision   Json?                         // 最终决策
  executedTradeId String?                       // 关联交易
  totalCost       Decimal  @default(0) @db.Decimal(10, 4) // LLM 成本
  createdAt       DateTime @default(now())
  completedAt     DateTime?
  @@map("ai_research_sessions")
}

// 产品 B：AI 策略
model AiStrategy {
  id                String   @id @default(uuid())
  userId            String   @map("user_id")
  name              String
  strategyType      String   @default("normal") // normal/grid
  tradingMode       String   @default("solo")   // solo/debate
  coinSourceConfig  Json                        // 币种来源配置
  indicatorConfig   Json     @default("{}")     // 指标开关
  riskControlConfig Json     @default("{}")     // 风控参数
  exchangeApiKeyId  String                      // 交易所 API Key
  intervalMinutes   Int      @default(60)       // 运行间隔
  amountPerTrade    Decimal  @default(50) @db.Decimal(18, 8)
  isActive          Boolean  @default(false)
  status            String   @default("stopped") // running/stopped/paused
  evolutionTier     Int      @default(2)        // Sharpe 进化层级
  rollingSharpe     Decimal? @db.Decimal(10, 4)
  totalTrades       Int      @default(0)
  totalPnl          Decimal  @default(0) @db.Decimal(18, 8)
  winRate           Decimal? @db.Decimal(5, 2)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  @@map("ai_strategies")
}

// 产品 B：策略决策日志
model AiStrategyLog {
  id            String   @id @default(uuid())
  strategyId    String   @map("strategy_id")
  symbol        String
  action        String   // open_long/open_short/close_long/close_short/hold/skip
  confidence    Decimal? @db.Decimal(5, 2)
  reasoning     String?  @db.Text
  decisionMode  String   // solo/debate
  executed      Boolean  @default(false)
  positionId    String?
  error         String?
  createdAt     DateTime @default(now())
  @@map("ai_strategy_logs")
}
```

#### 8.0.6 API 端点

**产品 A — AI 研究团队：**
```
POST   /ai/research/start        # 触发研究（symbol, depth, autoExecute）
GET    /ai/research/:id/status    # 查询研究进度（WebSocket 实时推送）
GET    /ai/research/:id/report    # 获取完整研究报告
POST   /ai/research/:id/execute   # 手动执行交易
GET    /ai/research/history        # 研究历史列表
```

**产品 B — AI 自动交易：**
```
POST   /ai/strategy               # 创建 AI 策略
GET    /ai/strategy                # 我的策略列表
GET    /ai/strategy/:id            # 策略详情 + 运行状态
PUT    /ai/strategy/:id            # 更新策略配置（支持热更新）
DELETE /ai/strategy/:id            # 删除策略
POST   /ai/strategy/:id/start     # 启动策略
POST   /ai/strategy/:id/stop      # 停止策略
POST   /ai/strategy/:id/pause     # 暂停策略
GET    /ai/strategy/:id/logs      # 策略决策日志
GET    /ai/strategy/competition    # 竞赛排行榜
```

**共享 API：**
```
GET    /ai/config                  # 获取 AI 配置
PUT    /ai/config                  # 更新 AI 配置
GET    /ai/performance             # AI 交易性能统计
GET    /ai/budget                  # 当月 LLM 预算
```

#### 8.0.7 实施阶段（19 个原子任务）

**Phase 8.0-A：基础层搭建（6 任务）**

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 1 | 复制 v2-dev 可复用服务 | 13 个文件复制 + 修 import | [ ] |
| 2 | 修复 market-data.service | 修复 fetchOHLCV 方法签名 | [ ] |
| 3 | 补完 safety.service L5 | 实现回撤计算逻辑 | [ ] |
| 4 | **新建 ai-execution.service** | **AI 执行逻辑（核心）** | [ ] |
| 5 | Prisma Schema 扩展 + 迁移 | 新增 3 表 + 扩展已有表 | [ ] |
| 6 | 重写 ai.module 注册 | 双产品模块 + 依赖注入 | [ ] |

**Phase 8.0-B：产品 A — AI 研究团队（5 任务）**

| # | 任务 | 来源 | 状态 |
|---|------|------|------|
| 7 | 新建 crypto-analysts.service | 4 类 AI 分析师 | [ ] |
| 8 | 新建 risk-debate.service | 三方风控辩论 | [ ] |
| 9 | 新建 research-pipeline.service | 5 阶段流水线编排 | [ ] |
| 10 | 新建 research-reflection.service | 交易后反思 → 存入记忆 | [ ] |
| 11 | 产品 A API 端点 | 5 个接口 | [ ] |

**Phase 8.0-C：产品 B — AI 自动交易（6 任务）**

| # | 任务 | 来源 | 状态 |
|---|------|------|------|
| 12 | 新建 strategy-engine.service | 策略 CRUD + 配置管理 | [ ] |
| 13 | 新建 coin-scanner.service | 币种来源配置 | [ ] |
| 14 | 新建 consensus.service | 多 AI 共识投票 | [ ] |
| 15 | 新建 auto-trader.service | AI 自动交易循环 | [ ] |
| 16 | 新建 grid-trading.service | 网格交易策略 | [ ] |
| 17 | 产品 B API 端点 | 10 个接口 | [ ] |

**Phase 8.0-D：监控 + 测试（2 任务）**

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 18 | 共享 API + 监控 + WebSocket | 配置/性能/回撤监控/实时推送 | [ ] |
| 19 | 集成测试 + 冒烟验证 | 两产品端到端验证 | [ ] |

**Phase 8.0-E：前端 UI（后续）**

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 20 | 产品 A 页面 | 研究入口、进度页、报告页、历史 | [ ] |
| 21 | 产品 B 页面 | 策略列表、创建向导、详情、辩论、竞赛 | [ ] |
| 22 | 共享 AI 设置页 | LLM 配置、预算管理 | [ ] |

**预计总工期**：后端 15-20 天，前端 5-8 天

---

### 8.1 DEX 永续合约交易所接入

> **目标**：支持链上 DEX 永续合约交易，扩展交易场景
> **开发原则**：使用交易所官方 SDK/API，不自己实现交易协议
> **目标 DEX**：Hyperliquid、Aster DEX、Lighter

#### 8.1.1 当前现状

| 类型 | 支持情况 | 说明 |
|------|----------|------|
| CEX (中心化交易所) | ✅ 已支持 | 基于 **CCXT** 开源库 (Binance/OKX/Bybit) |
| DEX 永续合约 | ❌ 待开发 | 需接入各 DEX 官方 API |

#### 8.1.2 目标 DEX 选型

| DEX | 类型 | 链 | API 方式 | 推荐度 |
|-----|------|-----|---------|--------|
| **Hyperliquid** | 永续合约 | Hyperliquid L1 | REST + WebSocket API | ⭐⭐⭐ 首选 |
| **Aster DEX** | 永续合约 | 多链 | REST API | ⭐⭐ |
| **Lighter** | 永续合约 | 多链 | REST API | ⭐⭐ |

> **注意**：这些 DEX 主要使用 REST API 方式（类似 CEX），不像 Uniswap 需要链上签名。
> 用户提供 API Key 或钱包签名授权即可，复用已有的 ApiKey 管理体系。

#### 8.1.3 数据库扩展

```prisma
// 扩展 ApiKey 模型，支持 DEX 钱包认证
// 在现有 ApiKey 表增加字段：
//   authType      String   @default("api_key") // "api_key" | "wallet"
//   walletAddress String?  // DEX 钱包地址

// DEX 交易记录
model DexTrade {
  id            String   @id @default(uuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])

  dex           String   // hyperliquid, aster, lighter
  symbol        String   // BTC-PERP
  side          String   // long, short
  orderType     String   // market, limit

  amount        Decimal  @db.Decimal(18, 8)
  price         Decimal? @db.Decimal(18, 8)
  filledPrice   Decimal? @db.Decimal(18, 8)

  txHash        String?
  orderId       String?  // DEX 返回的订单 ID
  status        String   @default("pending") // pending, filled, failed, cancelled

  signalId      String?
  aiAnalysisId  String?

  createdAt     DateTime @default(now())
  filledAt      DateTime?

  @@map("dex_trades")
}
```

#### 8.1.4 任务清单

| # | 任务 | 预估 | 状态 |
|---|------|------|------|
| 1 | DEX 适配器接口设计 | 0.5天 | [ ] |
| 2 | Hyperliquid 适配器 | 2天 | [ ] |
| 3 | Aster DEX 适配器 | 1.5天 | [ ] |
| 4 | Lighter 适配器 | 1.5天 | [ ] |
| 5 | ApiKey 扩展 (wallet auth) | 0.5天 | [ ] |
| 6 | 前端 DEX 交互 UI | 1天 | [ ] |

**预计总工期**：6-8 天

---

### 8.2 生态中心扩展

> **目标**：完善 HOOT 代币经济生态
> **开发原则**：基于成熟开源合约，不自己造轮子

#### 8.2.1 已完成功能（基于开源项目）

| 功能 | 状态 | 开源基础 |
|------|------|----------|
| HOOT ERC-20 合约 | ✅ 已完成 | [OpenZeppelin ERC20](https://github.com/OpenZeppelin/openzeppelin-contracts) |
| 质押分红合约 | ✅ 已完成 | [Synthetix StakingRewards](https://github.com/Synthetixio/synthetix) |
| 空投管理 | ✅ 后端完成 | 自研（基于 Merkle Tree 模式）|
| 锁仓释放 | ✅ 已完成 | 线性释放机制 |
| 权重计算 (1.0x-3.0x) | ✅ 已完成 | 基于 Synthetix 模式 |

#### 8.2.2 质押增强（待开发）

| 功能 | 开源方案 | 状态 |
|------|----------|------|
| 多池质押 | [SushiSwap MasterChef](https://github.com/sushiswap/sushiswap) | [ ] |
| LP 质押 | [Uniswap V2 LP](https://github.com/Uniswap/v2-core) | [ ] |
| 自动复投 | [Yearn Vault](https://github.com/yearn/yearn-vaults) | [ ] |
| veToken 锁仓 | [Curve veCRV](https://github.com/curvefi/curve-dao-contracts) | [ ] |

#### 8.2.3 治理系统 (DAO)

| 功能 | 开源方案 | 状态 |
|------|----------|------|
| 治理框架 | [OpenZeppelin Governor](https://github.com/OpenZeppelin/openzeppelin-contracts) | [ ] |
| 时间锁 | OpenZeppelin TimelockController | [ ] |
| 投票委托 | OpenZeppelin Votes | [ ] |
| 提案执行 | OpenZeppelin Governor | [ ] |

**推荐方案**：直接使用 OpenZeppelin Governor 全套，已久经考验

#### 8.2.4 NFT 会员系统

| 功能 | 开源方案 | 状态 |
|------|----------|------|
| NFT 合约 | [OpenZeppelin ERC721](https://github.com/OpenZeppelin/openzeppelin-contracts) | [ ] |
| NFT 市场 | [Seaport (OpenSea)](https://github.com/ProjectOpenSea/seaport) | [ ] |
| NFT 质押 | [BAYC ApeCoin Staking](https://github.com/ApeFinance/ape-staking) | [ ] |

---

### 8.3 第二阶段开发优先级

```
优先级排序：

1. AI 智能交易 (8.0) - 最高优先级 ★★★★ 🔥 当前进行中
   └── 产品 A：AI 研究团队（5 阶段多智能体深度研究 → 交易决策）
   └── 产品 B：AI 自动交易（策略配置 → Solo/Debate → 自动循环交易）
   └── 执行层：独立 CCXT 实例，AI 完整执行逻辑
   └── 预计 22 个原子任务，5 个子阶段

2. DEX 接入 (8.1) - 高优先级 ★★★
   └── 核心功能扩展，支持链上永续合约交易
   └── Hyperliquid / Aster DEX / Lighter
   └── 预计 6-8 天

3. 质押增强 (8.2.2) - 中优先级 ★★
   └── 增强代币经济
   └── 预计 1-2 周

4. 治理系统 (8.2.3) - 低优先级 ★
   └── 社区治理，可延后
   └── 预计 2-3 周

5. NFT 系统 (8.2.4) - 延后 ☆
   └── 非核心功能
   └── 预计 2-3 周
```

---

### 8.4 开源项目参考资源

#### DEX 接入

| 项目 | GitHub | 说明 |
|------|--------|------|
| 1inch Fusion SDK | [1inch/fusion-sdk](https://github.com/1inch/fusion-sdk) | DEX 聚合器，推荐 |
| Uniswap SDK | [Uniswap/sdks](https://github.com/Uniswap/sdks) | 官方 SDK |
| 0x Protocol | [0xProject/0x-api](https://github.com/0xProject/0x-api) | 聚合器 |
| ParaSwap | [paraswap/paraswap-sdk](https://github.com/paraswap/paraswap-sdk) | 聚合器 |

#### 质押与代币经济

| 项目 | GitHub | 说明 |
|------|--------|------|
| Synthetix StakingRewards | [Synthetixio/synthetix](https://github.com/Synthetixio/synthetix) | ✅ 已使用 |
| SushiSwap MasterChef | [sushiswap/sushiswap](https://github.com/sushiswap/sushiswap) | 多池质押 |
| Curve veCRV | [curvefi/curve-dao-contracts](https://github.com/curvefi/curve-dao-contracts) | 锁仓模式 |
| Yearn Vaults | [yearn/yearn-vaults](https://github.com/yearn/yearn-vaults) | 自动复投 |

#### 治理 (DAO)

| 项目 | GitHub | 说明 |
|------|--------|------|
| OpenZeppelin Governor | [OpenZeppelin/contracts](https://github.com/OpenZeppelin/openzeppelin-contracts) | 治理框架，推荐 |
| Compound Governor | [compound-finance/compound-protocol](https://github.com/compound-finance/compound-protocol) | 经典治理 |
| Snapshot | [snapshot-labs/snapshot](https://github.com/snapshot-labs/snapshot) | 链下投票 |

#### NFT

| 项目 | GitHub | 说明 |
|------|--------|------|
| OpenZeppelin ERC721 | [OpenZeppelin/contracts](https://github.com/OpenZeppelin/openzeppelin-contracts) | NFT 标准 |
| Seaport (OpenSea) | [ProjectOpenSea/seaport](https://github.com/ProjectOpenSea/seaport) | NFT 市场 |

#### 基础库

| 项目 | GitHub | 说明 |
|------|--------|------|
| ethers.js | [ethers-io/ethers.js](https://github.com/ethers-io/ethers.js) | Web3 库 |
| viem | [wagmi-dev/viem](https://github.com/wagmi-dev/viem) | 轻量 Web3 |
| wagmi | [wagmi-dev/wagmi](https://github.com/wagmi-dev/wagmi) | ✅ 已使用 |
| CCXT | [ccxt/ccxt](https://github.com/ccxt/ccxt) | ✅ 已使用 (CEX) |

---

## Phase 12（后续待完善）：网格策略进阶风控

| 功能 | 说明 | 复杂度 |
|------|------|--------|
| Order Book Depth 检测 | 盘口深度薄时缩仓，需扩展 fetchOrderBook API | 高 |
| RSI 背离后端检测 | 价格新高但 RSI 不新高 → 趋势破坏信号 | 低 |

---

## 版本记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v2.2 | 2026-03-05 | Phase 11 Round 1+2 nofx 对齐：ATR 追踪网格宽度、量能骤变方向信号、OI 真假突破确认、bid/ask Spread 检测、ATR Spike 检测，完成 TypeScript 编译验证 |
| v2.1 | 2026-02-10 | Phase 8.0 AI 模块方案定稿：双产品架构（产品 A 研究团队 + 产品 B 自动交易）；AI 独立执行层（独立 CCXT，不走 BullMQ）；v2-dev 13 文件直接复用 + 10 文件新建；22 原子任务 5 子阶段 |
| v2.0 | 2026-02-06 | 新增第二阶段规划：DEX 接入、生态中心扩展（Phase 8）；明确"开源优先"原则，补充开源项目参考 |
| v1.9 | 2026-01-31 | 辅助功能完善：Web3 钱包连接 (wagmi + WalletConnectModal)、TG Bot 推送通知完成、管理后台 API 对接完成、首页点击问题修复 |
| v1.8 | 2026-01-30 | 风控系统完成：RiskControlService（持仓数/同币种/日限/余额检查）+ RiskLog 模型 + 用户友好错误消息 |
| v1.7 | 2026-01-30 | 🎉 全部开发完成：单元测试、E2E 测试、部署脚本、生产环境配置、监控告警 |
| v1.6 | 2026-01-30 | 前端对接完成：所有页面与后端 API 对接完成，健康检查验证通过 |
| v1.5 | 2026-01-30 | 智能合约开发完成：HOOT ERC-20、StakingRewards 质押合约、部署脚本、54 个测试通过 |
| v1.4 | 2026-01-30 | 完善后端功能：Webhook 签名验证、交易手续费计算、通知推送集成、BillingLog 模型 |
| v1.3 | 2026-01-30 | Phase 3-7 后端全部完成：新增 WebSocket、通知系统、健康检查、信号追踪模块 |
| v1.2 | 2026-01-28 | 明确 CCXT 架构：利用 CCXT 内置能力处理交易细节，不复制 Freqtrade 代码 |
| v1.1 | 2026-01-28 | 调整开发顺序，核心交易系统 MVP 优先 |
| v1.0 | 2026-01-28 | 初始版本，基于项目需求 v6.2 |
