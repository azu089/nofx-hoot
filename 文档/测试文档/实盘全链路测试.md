# 实盘全链路端到端测试

> 目标：从管理员上传策略 → 用户选择 → 配置 → 回测 → 部署 → 交易 → 扣费 的全流程验证

---

## 当前系统状态

| 组件 | 状态 | 说明 |
|------|------|------|
| VPS | ✅ 运行中 | IP: 159.89.0.87 (法兰克福) |
| Freqtrade | ✅ 运行中 | 版本 2025.12, 心跳正常 |
| 当前策略 | SampleStrategy | 默认策略 |
| 交易模式 | ⚠️ dry_run=true | 需通过部署流程改为 false |

---

## 测试策略代码

> 此策略专门用于链路测试，快速触发交易，小额控制风险

```python
"""
QuantFi 实盘测试策略 - 仅用于验证交易流程
注意：此策略仅用于测试，不适合实际交易使用
"""
from freqtrade.strategy import IStrategy
from pandas import DataFrame
import talib.abstract as ta


class TestStrategy(IStrategy):
    """
    极简测试策略 - 用于验证交易链路
    - 买入条件：RSI < 40（容易触发）
    - 卖出条件：RSI > 60 或 盈利 1%
    - 止损：2%
    - 每单：10 USDT
    """

    INTERFACE_VERSION = 3

    # 最小盈利目标 1%
    minimal_roi = {
        "0": 0.01  # 1% 盈利就卖
    }

    # 止损 2%
    stoploss = -0.02

    # 5分钟K线
    timeframe = '5m'

    # 启动需要的K线数量
    startup_candle_count = 30

    # 是否可以做空（否）
    can_short = False

    # 交易数量控制
    max_open_trades = 1

    def populate_indicators(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """计算技术指标"""
        # RSI 指标
        dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)

        return dataframe

    def populate_entry_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """买入信号：RSI < 40（容易触发）"""
        dataframe.loc[
            (dataframe['rsi'] < 40),
            'enter_long'
        ] = 1

        return dataframe

    def populate_exit_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """卖出信号：RSI > 60"""
        dataframe.loc[
            (dataframe['rsi'] > 60),
            'exit_long'
        ] = 1

        return dataframe
```

---

## 完整测试流程（10步）

### Phase 1: 环境准备

#### Step 1: 准备工作（前置检查）

**检查清单**:
- [ ] Binance 账户有 ≥ 50 USDT
- [ ] API Key 有现货交易权限
- [ ] 平台账户有充足点卡
- [ ] VPS 实例运行中

---

### Phase 2: 管理员上传策略

#### Step 2: 管理后台上传测试策略

**页面**: `/admin/strategies`

**操作步骤**:
1. 使用管理员账号登录
2. 进入 **策略管理** 页面
3. 点击 **"添加策略"** 按钮
4. 填写表单：
   - 策略名称：`实盘测试策略`
   - 策略类型：`趋势策略` (trend)
   - 风险等级：`中风险` (medium)
   - 策略描述：`用于验证交易链路的测试策略，RSI 超卖买入，快速止盈`
   - 策略代码：粘贴上方的 Python 代码
5. 点击 **"创建"**
6. 在策略卡片点击 **"上架"** 使其可见

**验证点**:
- [ ] 策略创建成功提示
- [ ] 策略状态显示"已上线"
- [ ] 策略出现在策略列表中

**代码路径**:
- 前端: `apps/web/src/app/(admin)/admin/strategies/page.tsx`
- API: `adminApi.createStrategy()` → `adminApi.toggleStrategyStatus()`

---

### Phase 3: 用户配置策略

#### Step 3: 用户浏览策略市场

**页面**: `/strategies`

**操作步骤**:
1. 以普通用户登录
2. 进入 **策略市场** 页面
3. 使用筛选器：来源 → 官方
4. 找到 **"实盘测试策略"**
5. 点击 **"详情"** 查看

**验证点**:
- [ ] 策略在市场中可见
- [ ] 显示回测数据（胜率、回撤、夏普）
- [ ] 可点击进入详情

---

#### Step 4: 配置策略参数并回测

**页面**: `/trading/backtest?strategyId=xxx`

**操作步骤**:
1. 在策略市场点击 **"回测"** 按钮
2. 配置参数：
   - 投入资本: **10 USDT**（最小测试金额）
   - 杠杆: **1x**
   - 止损: **-2%**
   - 止盈: **1%**
   - 最大持仓数: **1**
   - K线周期: **5m**
   - 交易对: **BTC/USDT**
3. 点击 **"开始回测"**
4. 等待回测完成，查看结果

**验证点**:
- [ ] 参数输入正常
- [ ] 回测成功执行
- [ ] 显示回测收益曲线
- [ ] 显示交易次数、胜率、回撤

---

#### Step 5: 保存到我的策略

**页面**: `/trading/backtest` → `/strategies/my`

**操作步骤**:
1. 回测完成后，点击 **"保存到我的策略"**
2. 确认保存
3. 自动跳转到 **"我的策略"** 页面

**验证点**:
- [ ] 保存成功提示
- [ ] 策略出现在"我的策略"列表
- [ ] 显示配置的参数

---

### Phase 4: 部署策略

#### Step 6: 启动前检查

**页面**: `/strategies/my`

**操作步骤**:
1. 找到保存的策略配置
2. 点击 **"启动"** 按钮
3. 查看启动前检查结果

**检查项目**:
| 检查项 | 预期状态 |
|--------|---------|
| VPS 实例状态 | ✅ 运行中 |
| API Key 状态 | ✅ 已配置 |
| 交易所余额 | ✅ 足够（显示金额） |
| 策略冲突 | ✅ 无冲突 |

**如果 API Key 未配置**:
1. 跳转 `/wallet/api-keys`
2. 添加 Binance API Key
3. 返回重新检查

**代码路径**:
- 前端: `apps/web/src/app/(dashboard)/strategies/my/page.tsx` 第 212-267 行
- 检查逻辑: `runPreflightChecks()` 函数

---

#### Step 7: 部署策略到 VPS（关键步骤）

**操作步骤**:
1. 启动前检查全部通过后
2. 点击 **"确认启动"**
3. 等待部署完成

**后端执行流程**:
```
1. StrategyDeployService.deployStrategy()
2. 获取用户配置 (user_strategy_configs)
3. 获取并解密 API Key (AES-256-GCM)
4. 生成 Freqtrade 配置 (dry_run: false ← 关键！)
5. 上传策略代码到 VPS (8081端口代理)
6. 重启 Freqtrade 容器
7. 更新配置状态 (is_active: true)
```

**验证点**:
- [ ] 部署成功提示
- [ ] 策略状态变为"运行中"
- [ ] VPS 日志显示 "Using strategy TestStrategy"

**关键配置确认**:
```bash
# SSH 到 VPS 验证
ssh root@159.89.0.87
docker exec freqtrade cat /freqtrade/user_data/config.json | jq '.dry_run'
# 应该返回: false
```

**代码路径**:
- 部署服务: `apps/api/src/modules/strategies/strategy-deploy.service.ts` 第 133-288 行
- 配置生成: 第 362 行 `dry_run: customConfig.dry_run ?? false`

---

### Phase 5: 监控交易

#### Step 8: 监控交易执行

**页面**: `/trading`

**操作步骤**:
1. 进入 **交易控制台**
2. 查看策略运行状态
3. 查看实时日志流
4. 等待交易信号触发

**触发条件**:
- RSI < 40 时买入
- RSI > 60 时卖出，或盈利 1% 自动平仓

**验证点**:
- [ ] 显示策略运行状态：运行中
- [ ] 日志实时更新
- [ ] 当有信号时显示持仓
- [ ] 交易所账户有对应订单

---

#### Step 9: 查看交易历史

**页面**: `/trading/history`

**操作步骤**:
1. 进入 **交易历史** 页面
2. 查看最新交易记录
3. 筛选盈亏状态

**验证点**:
- [ ] 交易记录同步到平台
- [ ] 显示开仓价/平仓价
- [ ] 显示盈亏金额和百分比
- [ ] 显示手续费和燃油费

---

### Phase 6: 紧急操作与扣费

#### Step 10: 测试紧急平仓

**方式 A - 悬浮按钮**:
1. 在 `/trading`、`/dashboard`、`/instances` 页面
2. 点击右下角红色 **紧急按钮**
3. 展开查看运行中策略数量
4. 点击 **"一键停止 & 平仓"**
5. 确认对话框中点击 **"确认紧急停止"**

**方式 B - 设置页面**:
1. 进入 `/settings/panic`
2. 点击 **"紧急停止"**
3. 确认操作

**验证点**:
- [ ] 所有持仓立即平仓
- [ ] 所有策略停止运行
- [ ] 状态实时更新为"已停止"
- [ ] 交易所无剩余持仓

**代码路径**:
- 悬浮按钮: `apps/web/src/components/features/trading/PanicButton.tsx` 第 60-97 行
- API: `instancesApi.forceExit()` → `instancesApi.stop()`

---

#### Step 11: 验证扣费流程

**页面**: `/wallet/billing`

**操作步骤**:
1. 进入 **账单明细** 页面
2. 筛选 **"燃油费"** 类型
3. 查看最新扣费记录

**验证点**:
- [ ] 有 `gas_fee` 类型记录
- [ ] 扣费金额 = 盈利 × 20%
- [ ] 钱包余额相应减少

**计算验证**:
```
假设：平仓盈利 $1.00
燃油费 = $1.00 × 20% = $0.20
```

---

## 验证检查清单

### 功能验证

| # | 功能 | 验证方式 | 状态 |
|---|------|---------|------|
| 1 | 管理员上传策略 | 策略市场可见 | ⬜ |
| 2 | 用户浏览策略 | 列表/筛选正常 | ⬜ |
| 3 | 回测功能 | 回测结果正确 | ⬜ |
| 4 | 保存配置 | 我的策略显示 | ⬜ |
| 5 | 启动前检查 | 4项检查通过 | ⬜ |
| 6 | 策略部署 | VPS 日志显示加载 | ⬜ |
| 7 | 实盘模式 | dry_run=false | ⬜ |
| 8 | 交易执行 | 交易所有订单 | ⬜ |
| 9 | 数据同步 | 历史记录可见 | ⬜ |
| 10 | 紧急平仓 | 一键操作生效 | ⬜ |
| 11 | 燃油费扣除 | billing 有记录 | ⬜ |

### 边界测试

| # | 场景 | 预期行为 |
|---|------|---------|
| 1 | 无 API Key 启动 | 显示"请先配置 API Key" |
| 2 | VPS 未运行启动 | 显示"请先启动 VPS 实例" |
| 3 | 余额不足启动 | 显示"交易所余额不足" |
| 4 | 已有运行策略 | 显示"有其他运行中的策略" |
| 5 | 无持仓平仓 | 提示"当前无持仓" |

---

## 风险控制

| 风险 | 控制措施 |
|------|---------|
| 资金损失 | 测试金额限制 10 USDT |
| 频繁交易 | max_open_trades = 1 |
| 大额亏损 | stoploss = -2% |
| 操作失误 | 所有危险操作需二次确认 |

---

## 回滚方案

### 策略回滚
```bash
# SSH 到 VPS
ssh root@159.89.0.87

# 停止策略
docker exec freqtrade freqtrade stopbot

# 切换回模拟模式
docker exec freqtrade cat /freqtrade/user_data/config.json | \
  jq '.dry_run = true' > /tmp/config.json
docker cp /tmp/config.json freqtrade:/freqtrade/user_data/config.json

# 重启
docker restart freqtrade
```

### 数据库回滚
```sql
-- 重置配置状态
UPDATE user_strategy_configs SET is_active = false WHERE user_id = 'xxx';
```

---

## 关键代码文件清单

| 模块 | 文件 | 说明 |
|------|------|------|
| 管理员策略 | `apps/web/src/app/(admin)/admin/strategies/page.tsx` | 上传/管理 |
| 策略市场 | `apps/web/src/app/(dashboard)/strategies/page.tsx` | 列表/筛选 |
| 回测配置 | `apps/web/src/app/(dashboard)/trading/backtest/page.tsx` | 参数配置 |
| 我的策略 | `apps/web/src/app/(dashboard)/strategies/my/page.tsx` | 启动/检查 |
| 策略部署 | `apps/api/src/modules/strategies/strategy-deploy.service.ts` | 部署逻辑 |
| 交易历史 | `apps/web/src/app/(dashboard)/trading/history/page.tsx` | 记录展示 |
| 紧急平仓 | `apps/web/src/components/features/trading/PanicButton.tsx` | 悬浮按钮 |
| 账单明细 | `apps/web/src/app/(dashboard)/wallet/billing/page.tsx` | 扣费记录 |
