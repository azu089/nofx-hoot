# 策略详情页完善 - 验收文档

## 完成时间
2025-12-29

## 改动概述

完善 `apps/web/src/app/(dashboard)/strategies/[id]/page.tsx`，添加以下功能：

### 1. 收益曲线图（Recharts）
- ✅ 使用 AreaChart 展示策略历史收益
- ✅ 支持时间范围切换（7天/30天/90天）
- ✅ 渐变填充效果（翡翠绿）
- ✅ 自定义 Tooltip 样式（深色主题）
- ✅ 响应式设计（高度 300px）

### 2. 回测数据展示
- ✅ 胜率（Target 图标）
- ✅ 最大回撤（TrendingDown 图标）
- ✅ 夏普比率（TrendingUp 图标）
- ✅ 平均持仓时间（Clock 图标）
- ✅ 4 个数据卡片，响应式网格布局

### 3. 策略配置面板（完善）
- ✅ 投入资本输入（带范围提示）
- ✅ 交易对选择（5 个主流币种）
- ✅ 止损设置（-100% ~ 0，带推荐）
- ✅ 杠杆倍数（仅合约策略显示，1-20x）
- ✅ 费用说明（VPS + 燃油费）
- ✅ 配置项禁用状态（已订阅后）

### 4. 订阅状态管理
- ✅ 根据 `is_subscribed` 字段切换按钮文案
- ✅ 未订阅：显示"订阅并启用策略"
- ✅ 已订阅：显示"前往交易控制台"和"返回仪表盘"
- ✅ 已订阅时禁用配置项编辑

## 技术实现

### 新增依赖
- Recharts（已安装）：用于收益曲线图

### 新增图标
- `Clock` - 平均持仓时间
- `Target` - 胜率
- `TrendingDown` - 最大回撤
- `Activity` - 回测数据标题

### 类型扩展
```typescript
type StrategyDetail = Strategy & {
  content?: string;
  is_subscribed?: boolean;
};
```

### 模拟数据
- 收益曲线数据：根据时间范围生成模拟数据
- 平均持仓时间：4.2h（后续需从后端获取）

## UI 风格

严格遵循 `UI_ARCHITECTURE.md` 规范：

- **背景**：主背景 #0B0E11，卡片 #131722
- **品牌色**：科技蓝 #3772FF
- **语义色**：
  - 盈利绿 #00C087
  - 亏损红 #F23645
  - 警告橙 #F7931A
- **圆角**：卡片 8px，输入框 6px
- **字体**：Inter，数字等宽

## 验收步骤

### 1. 启动服务
```bash
cd /Users/azu/主QuantFi/apps/web
pnpm dev
```

### 2. 访问页面
打开浏览器：http://localhost:3001/strategies/[任意策略ID]

例如：http://localhost:3001/strategies/1

### 3. 检查收益曲线图
- [ ] 曲线图正常渲染（绿色渐变区域）
- [ ] 时间范围按钮可点击切换（7天/30天/90天）
- [ ] 鼠标悬浮显示 Tooltip（深色背景，显示日期和收益）
- [ ] X 轴显示日期，Y 轴显示金额（$符号）

### 4. 检查回测数据
- [ ] 4 个数据卡片正常显示
- [ ] 胜率、最大回撤、夏普比率、平均持仓时间
- [ ] 图标和颜色符合语义（胜率绿色，回撤红色）

### 5. 检查配置面板
- [ ] 投入资本输入框（带最低金额提示）
- [ ] 交易对下拉菜单（5 个选项）
- [ ] 止损设置输入框（带范围提示）
- [ ] 杠杆倍数（仅高风险策略显示）
- [ ] 费用说明清晰（$25/月 + 20%抽成）

### 6. 检查订阅状态
- [ ] 未订阅时显示"订阅并启用策略"按钮
- [ ] 已订阅时显示"前往交易控制台"和"返回仪表盘"按钮
- [ ] 已订阅时配置项禁用（disabled）

### 7. 检查响应式
- [ ] 桌面端（>1024px）：左右布局，收益图占 2/3
- [ ] 平板/移动端（<1024px）：单列布局，配置面板在下方

## 已知限制

1. **模拟数据**：
   - 收益曲线数据为前端生成的模拟数据
   - 平均持仓时间为固定值 4.2h
   - 后续需要后端提供真实历史数据

2. **订阅状态**：
   - `is_subscribed` 字段需要后端在 `GET /strategies/:id` 接口返回
   - 目前使用类型断言处理

## 风险点

- 无破坏性变更
- 不影响现有订阅流程
- 纯前端 UI 增强，无后端依赖

## 回滚方案

```bash
cd /Users/azu/主QuantFi
git checkout apps/web/src/app/\(dashboard\)/strategies/\[id\]/page.tsx
```

## 下一步建议

1. **后端增强**：
   - 在 `GET /strategies/:id` 接口返回 `is_subscribed` 字段
   - 提供策略历史收益数据 API（`GET /strategies/:id/profit-history?range=30d`）
   - 添加 `avg_hold_time` 字段到回测数据

2. **前端优化**：
   - 添加收益曲线数据的 React Query 缓存
   - 收益为负时使用红色曲线
   - 添加收益率（百分比）展示

3. **交互优化**：
   - 点击曲线图某个点查看当日详细数据
   - 配置保存到用户偏好（localStorage）
   - 添加策略对比功能

## 截图位置

（待用户提供截图）

---

**开发完成** ✅
**编译通过** ✅
**服务运行** ✅
**等待验收** 🟡
